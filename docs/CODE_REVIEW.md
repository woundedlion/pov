# Holosphere / daydream — Comprehensive Code Quality Review

*Scope: the C++ rendering engine (`core/`, `effects/`, `hardware/`, `targets/`), the
WASM bridge and build/codegen tooling, the native test suite (`tests/`), and the
`daydream` web simulator (app, segmented-worker pipeline, geometry tools, JS tests).*

*Out of scope (excluded by request): `core/effects_legacy.h`, `core/rotate.h`,
`targets/Holosphere/Holosphere.ino`. Vendored trees (`.pio/`, `node_modules/`,
`three.js/`) and generated artifacts (`color_luts.h`, `reaction_graph.cpp`,
`holosphere_wasm.*`) were judged against their generator contracts, not line-reviewed.*

Method: the codebase was partitioned across 20 independent expert sub-reviews (8 core
C++, 3 effects, 1 hardware, 1 wasm/build, 2 native-test, 5 daydream), each reading every
in-scope file in full and cross-checking against the README architecture. Findings below
are the de-duplicated synthesis.

---

## 1. Executive Summary

This is an exceptionally well-engineered project. Across ~45 KLOC of C++ and ~14 KLOC of
JavaScript, **no Critical defects and no shipped correctness bugs in the hot rendering
paths were found.** The dominant qualities are rigor and self-documentation: numerical
degeneracies are systematically guarded, the fail-fast (`HS_CHECK`) doctrine is applied
consistently to cold paths, memory ownership is explicit, and nearly every non-obvious
decision carries an inline rationale. The documentation quality is the single most
remarkable trait — among the best seen in a systems codebase of this size.

The defects that exist are concentrated in four areas, none of which compromise the
device's shipped behavior:

1. **A handful of latent correctness edges** in math/animation/hardware that are
   currently unreachable or fail-fast rather than silently wrong (e.g. a repeat-seam
   substep miscount in `Motion`, a torn debug-telemetry read on Phantasm, slerp endpoint
   precision).
2. **Parameter-range / preset drift in the effects layer** — several GUI slider ranges do
   not bracket the values their own presets push through them.
3. **Web-simulator pipeline and lifecycle gaps** — a generation-fence race that can
   composite an empty frame, uncancelled animation loops in the geometry tools, and weak
   accessibility on custom controls.
4. **Test-coverage holes around integration and distributed-systems edges** — the two
   largest daydream modules (`driver.js`, `daydream.js`) are untested, the
   controller↔worker live-tuning path is uncovered, and the sync protocol's 6-bit counter
   wrap is structurally skipped by a scaled-down test config.

---

## 2. Quality Dimension Grades

### Overall

| Dimension | Grade | Rationale |
|---|---|---|
| Correctness | **A−** | No hot-path or shipped bugs; remaining issues are latent edges, fail-fast-guarded degeneracies, or documented trade-offs. |
| Architectural elegance | **A** | Clean shape/rasterizer split, variadic compile-time filter pipeline with ordering invariants as `static_assert`s, CRTP zero-overhead bases, single-block arena, X-macro single-sources-of-truth. |
| Interface expressiveness / API design | **A−** | Strongly typed, `[[nodiscard]]`, explicit lossy casts, borrow-vs-own split, type-erased callables with compile-time foot-gun rejection. Dinged by a few non-explicit ctors and positional preset tuples. |
| Numerical robustness | **A−** | Clamp-before-`acos`, NaN→hi-bound contract enforced at the toolchain level, sentinels for projective infinity. A couple of latent precision cliffs. |
| Memory safety | **A** | Wrap-proof bounds math, debug generation/rebind stamps catching arena dangles, RAII scratch with LIFO trap. |
| Concurrency & real-time safety | **A−** | Single-core relaxed-atomics double-buffer reasoning is rigorous and correct; the one weak spot is a torn debug-telemetry read and a web-pipeline fence race. |
| Performance | **A−** | Engine is allocation-free in hot loops with baked LUTs and early-outs; the geometry-tool *pages* (not the engine) carry the only real perf debt. |
| Documentation | **A+** | Outstanding and pervasive; rationale-first, invariant-annotated, honest about limitations. |
| Readability & naming | **A** | Precise, consistent, intent-revealing. A scattering of un-named magic constants is the only recurring blemish. |
| Error handling | **A−** | Reject-at-boundary + graceful-degrade vs fail-fast doctrine is coherent; a few silent paths (export detach, worker init failure). |
| Consistency | **A−** | Strong house style; localized drift (`assert` vs `HS_CHECK`, `cosf` vs `fast_cosf`, three "capture-start" idioms, slider-range vs preset). |
| Testability & test suite | **B+** | C++ suite is excellent (independent oracles, verified death harness); daydream suite leaves integration layers and the live-tuning path untested, and sync wrap seams are skipped. |
| Build & tooling | **A−** | Provenance-hashed codegen, anti-drift smoke/screenshot gates, honest CI-skip documentation; local `just` surface omits the smoke gate. |
| Accessibility (web tools) | **C** | Custom toggles / 2-D drag pads / canvas zoom are mouse-only, no ARIA or keyboard. |

### Per-component summary

| Component | Correctness | Design | Docs | Tests | Notes |
|---|---|---|---|---|---|
| `core/` math, geometry, color | A− | A | A+ | A− | Model subsystem; tiny precision/consistency nits. |
| `core/` memory & containers | A | A− | A+ | A− | Type-erasure const-qualifier hole; one `assert` outlier. |
| `core/` sdf, scan, plot, filter | A− | A | A | A− | No distance-math bugs; DRY + one inaccurate comment. |
| `core/` animation & transformers | B+ | A− | A | B | `Motion` repeat-seam baseline is the headline item. |
| `core/` mesh / conway / solids / spatial | A | A | A+ | A− | No half-edge or memory bugs; hash-collision is by-design. |
| `core/` engine infra & platform | A− | A | A | A | `EVERY_N_MILLIS` host-mock parity/hygiene. |
| `effects/` (27) | A− | B+ | A | B+ | Slider-range vs preset drift is the cross-cutting weakness. |
| `hardware/` drivers & sync | A− | A− | A | B | Telemetry torn read; timing margins asserted-in-prose not enforced. |
| `targets/wasm` + build/codegen | A− | A− | A− | A | Anti-drift is excellent; manual MeshOps embind list is the gap. |
| daydream app (`driver/daydream/state/...`) | A− | A | A | — | Zero-copy view lifecycle handled rigorously; untested. |
| daydream segmented workers | B+ | A− | A | partial | Generation-fence/`pendingFrame` race. |
| daydream geometry tools | B | B | A− | A− | Math faithful; page-level perf/lifecycle/a11y debt. |

---

## 3. Strengths

- **Documentation is the project's superpower.** Nearly every non-obvious branch, epsilon,
  ordering constraint, and trade-off is justified at the site, including ones easy to get
  wrong (signed-SMLAD unsoundness, float→int UB, gamut-clip vs per-channel clip, relaxed
  atomics on a single core, arena polarity in Conway compositions).
- **Fail-fast is applied with discipline**, not blanket: `HS_CHECK` (survives `NDEBUG`) on
  cold seams, stripped `assert` backed by a cold trap on hot paths, soft handling for
  genuinely transient conditions — and a death harness that asserts the traps *actually*
  fire (SIGILL / `STATUS_ILLEGAL_INSTRUCTION`) with constant-folding defeated.
- **Compile-time invariants over runtime hope**: the variadic `Pipeline` rejects illegal
  filter orderings as `static_assert`s; arena sizes are `static_assert`-pinned from
  `sizeof`; X-macros make resolutions/rosters single-sourced and drift-detected.
- **Numerical degeneracy handling is systematic** — every `acos`/`asin` clamped, every
  `sqrt` radicand floored, every divisor guarded, `normalized_or` at caller-uncontrolled
  edges, with the failure mode each guard prevents documented.
- **Anti-drift engineering** between C++ and the web layer: real-WASM parity tests,
  provenance-hashed generators with runtime parity assertions, and a smoke harness that
  enumerates effects × resolutions from the module itself rather than a hand-mirror.
- **Zero-copy correctness** in the simulator: Emscripten heap-growth view detachment is
  handled at one tested choke point with a loud alias-divergence assertion.

---

## 4. Prioritized List of Items to Fix

*One global sequential numbering, grouped by priority. Each item: `file:line — issue`.*
*No Critical-severity defects were found.*

### High

1. ❌ core/animation.h:1335-1363 — `Motion::step` derives the substep *count* from a freshly recomputed `t-1` span while applying deltas from the carried `prev_frame_` baseline; at a repeat seam (`t` rewinds, `t_prev=-1`) the path is sampled at an extrapolated off-path parameter, so motion-blur sub-sampling can be wrong on the first frame of each repeated cycle. → **Declined (wrong premise):** the off-path sample cannot occur. `Motion::step` calls `AnimationBase::step` (`t++`) as its *first* statement, so `t ≥ 1` by the time `t_prev = t-1` is computed — `t_prev ≥ 0` on every invocation, never `-1`. At the seam the Timeline rewinds `t` to 0 *after* the end-of-cycle step; the next step pre-increments to `t=1`, giving `t_prev=0`, an on-path sample (the path start). No parameter is ever extrapolated outside `[0,1]`. The remaining count-vs-baseline split (count from the point arc, deltas from the carried frame) is real but benign by design: for a closed path the seam delta is identity (seamless loop, per the in-code rationale); for an open arc the small count cleanly snaps all sub-frames to the start rather than smearing the inherent end→start teleport into a streak — the desirable behavior. No change.
2. ❌ core/3dmath.h:1218-1220 — Vector `slerp` drops the shared `1/sinθ` factor; with `fast_sinf`'s ~0.17% error the two endpoint terms are perturbed independently, biasing interpolated *direction* near `t≈0`/`t≈1` — affects every spline sample, the function's core use. → **Declined (wrong premise):** the dropped factor is a *common positive scalar* on both terms, and `1/sinθ` is positive throughout (θ is bounded into `(TOLERANCE, π−TOLERANCE)` by the two guards above), so `normalize((s1·v1 + s2·v2)/sinθ) = normalize(s1·v1 + s2·v2)` *exactly* — the result direction is bit-identical with or without it. The direction error near `t≈0/1` comes solely from `fast_sinf` evaluating `s1=fast_sinf((1−t)θ)` and `s2=fast_sinf(tθ)`, which are computed identically either way; the dropped scalar does not couple them. Restoring `1/sinθ` would not reduce the perturbation — it would only add a division by an *approximate* `fast_sinf(θ)` (its own error) near θ→π, which the current form deliberately avoids (see the in-code rationale at 1212-1217). No change.
3. ✅ core/inplace_function.h:122-148,196 — the converting-constructor invocability check tests a *non-const* lvalue while `operator()` is `const`-qualified, so the SFINAE guards the wrong qualifier (a const-only callable is wrongly rejected; a `const` instance can mutate captured state). Same const-erasure hole in concepts.h:130-139 `FunctionRef`. → **Split: FunctionRef tightened, inplace_function half declined.** *inplace_function (declined):* its `invoke` casts `storage_` to a *non-const* `C*` and calls the callable as a non-const lvalue, so the SFINAE's non-const `decay_t<C>&` check correctly matches what `invoke` does. The const `operator()` over a `mutable` buffer is deliberate `std::function` parity (documented at lines 95-96); a const-only callable is *accepted* (a const method is callable on a non-const lvalue), and a `const` instance mutating captured state is exactly `std::function`'s contract. No real qualifier hole. *concepts.h FunctionRef (fixed):* the const-lvalue ctor's constraint *did* mismatch its own thunk — it required `std::invocable<Callable, Args...>` (non-const) but the thunk invokes through `static_cast<const Callable*>` (const). Tightened the constraint to `std::invocable<const Callable&, Args...>` so a non-const-only callable (e.g. a `mutable` lambda passed as a const lvalue / temporary) is cleanly SFINAE-rejected instead of failing inside the thunk; const-invocable callables (every borrow that compiles today) are unaffected.
4. ✅ hardware/pov_segmented.h:351-376 (with pov_sync.h:1249) — `telemetry()` returns `const Telemetry&` aliasing the live ISR-mutated struct; the foreground `memcmp`/copy reads it across many non-atomic words with no IRQ-off bracket, so it can compare a half-updated block and latch a torn `last_tm`. The in-code comment claims a snapshot is taken, but none is — return by value or copy under `__disable_irq()`. → **Fixed:** the health-telemetry poll now takes its copy inside a brief `__disable_irq()`/`__enable_irq()` bracket (the documented mailbox-handoff idiom, spec §8.2), so the block is consistent and the change comparison can't mis-fire on a torn read. The window is debug-only, ~1 Hz, and a handful of aligned-word copies — far shorter than the per-tick handoff, so no real-time cost. Rewrote the now-stale "deliberate benign race" comment and added a `@warning` on `telemetry()` documenting that the returned reference aliases the live ISR block and device callers must snapshot it under IRQ-off.
5. ✅ segment_controller.js:402-404,700-705 — generation-fence race: a stale-generation render still in flight at `setResolution` time re-arms `pendingFrame=true` in its `.then` after the fence cleared it, so the next `tick()` composites all-null results into a cleared buffer; re-check `inflightGen === renderGen` before arming. → **Fixed** (daydream `4202fc0`): the `renderParallel().then()` callback now arms `pendingFrame` only when `inflightGen === renderGen`, so a `setResolution()` during an in-flight render leaves the compositor disarmed (its responses are dropped by the fence anyway) until the next current-generation render completes. `renderInFlight` still clears unconditionally so the next tick dispatches a fresh render.
6. ✅ tools/solids.html:886-1006 — `generateThumbnails()` is a synchronous loop running a full WASM build + triangulation + WebGL render + `toDataURL()` per registered solid on the main thread during `init()`, blocking first paint; the `async` keyword is misleading (nothing is awaited). Yield between iterations. → **Fixed** (daydream `01491ac`): each iteration now `await`s a macrotask at the top of the loop, spreading the per-solid work across the event loop so the page paints and stays responsive while the footer fills progressively (`init()` already calls it un-awaited). The large base preview is refreshed the moment its own thumbnail is generated, since `init()` now renders it before the deferred loop produces it.
7. ✅ tools/mobius.html:417,610-621 — during preset animation `onAnimate` runs `applyConfig` every frame, performing ~12 DOM text/style mutations per frame regardless of motion; cache and update only on change. → **Fixed** (daydream `a06949e`): each complex-plane control's `updateUI` now caches the last label text and dot `left`/`top` strings and writes the DOM only when the (quantized) value changes, so a steady or slow preset elides the redundant per-frame mutations. The shader uniforms still update every frame (the actual animation); the cache vars live in each control's own closure so the four pads stay independent.
8. ✅ tests/test_pov_sync.h:61-67,1493-1554 — the test config sets `revs_per_effect=40` (production 960), so the 6-bit `rev_count & 63` field never crosses the 63→0 seam during normal play and the mod-64 rev-resync fold is only exercised with a +2 slip; the wraparound case is untested. → **Fixed:** extracted the fold's signed-mod-64 arithmetic into a named `beacon_rev_resync_delta(beacon_rev_count, current)` helper (behavior-preserving — `handle_beacon_burst` now calls it) and added `test_rev_resync_fold`, which drives the 63↔0 seam deterministically: small slips across the wrap fold to small signed deltas (residue 62 vs beacon 0 → +2; residue 1 vs beacon 63 → −2), the `[-32, 31]` window endpoints, magnitude-independence (residue of rev 130/66), and an exhaustive check that `(current + delta) mod 64 == beacon` for every beacon residue across the seam. The Sim-level wrap during normal play is covered by finding 9.
9. ✅ tests/test_pov_sync.h (whole) — no test drives `rev_count`/`effect_index` rolling through their 6-bit wrap *within* a single effect (the very ambiguity the `beacon_period_revs < 32` rule exists to resolve); dynamic wrap resolution ships unverified. → **Fixed:** added `test_sim_rev_wrap_within_effect` — a 4-board sim with `revs_per_effect=90` so `rev_in_effect` rolls through its 6-bit residue (rev 64) mid-effect during *normal* play. It pins, across the seam, that every board's `rev_in_effect` tracks the master exactly, the `&63` cross-check raises no spurious mismatch (`beacon_rev_mismatches==0`), phase/frame lockstep holds, and the next epoch still commits in lockstep (j-inference reads a wrapped rev). Verified non-vacuous by mutation: dropping `&63` from the cross-check comparison fails it (spurious mismatches climb) and dropping it from the resync fold fails it (counter diverges 64→32) — both pass without the test.
10. ✅ daydream/tests/segment_controller.test.js — the controller's `setEffect`/`setParameter`/`setAnimationsPaused` broadcast paths and `_snapshotParams()` are untested; a worker that drops or mis-orders the setEffect-then-params replay renders a *different effect than the main thread* — the exact divergence segment mode exists to catch — with no test. → **Fixed** (daydream `4b853ad`): added unit tests asserting `_snapshotParams()` flattens bool→1/0 and passes numbers through (and is empty with no engine); `setEffect` broadcasts `{name, params-snapshot}` to every worker in a *single* message (so the rebuild-to-defaults and param re-apply can't be split/reordered in transit); and `setParameter`/`setAnimationsPaused` broadcast their payloads to every worker (the latter also recording the flag).
11. ✅ daydream/tests/segment_worker.test.js — the worker's `setEffect`/`setParameter`/`setAnimationsPaused` handlers (incl. the "params applied AFTER rebuild" invariant) are untested; receiver half of #10, and it is pure DOM-free logic that is cheap to cover. → **Fixed** (daydream `6b32b4b`): the test's `FakeEngine.setEffect` now models the real rebuild-to-defaults by clearing params, making the ordering observable. Added tests that `init` and `setEffect` both re-apply the carried snapshot *after* the rebuild (a before-rebuild apply would be wiped); `setEffect` with no params leaves defaults and still posts `effectReady`; and `setParameter`/`setAnimationsPaused` forward to the engine (paused in both directions).

### Medium

12. ✅ core/geometry.h:726-727 — `Orientation::upsample`'s `count>=2` invariant rests on three separate facts (`CAPACITY>=1`, the early-return, ctor-runs-`set`); a future code path bypassing `set()` hits `0/0`. Add a single guard. → **Fixed:** added an explicit `if (count < 2) return;` guard immediately before the interpolation loop, so the `count-1` divisor is structurally guaranteed `>= 1` regardless of `num_frames`. No behavior change on any currently-reachable path (`count==1` already early-returned via `num_frames >= count`).
13. ✅ core/3dmath.h:1042 — public `make_rotation(axis, θ)` traps on `(zero-axis, π)` via the trailing `normalized()`; the doc implies a softer contract. Document the non-zero precondition or route through `normalized_or`. → **Fixed (documented):** added an `@pre` clause stating `axis` must be non-zero unit, explaining the `(zero-axis, π)` zero-magnitude collapse and that the `normalized()` trap is deliberate fail-fast (not a soft fallback), so callers that may produce a degenerate axis must guard it. Kept the trap — routing through `normalized_or` would mask a precondition violation, contrary to the project's fail-fast doctrine.
14. ✅ core/memory.h:993 — `Persist::~Persist` requires `T` default-constructible and assignable, but the `Cloneable` concept only requires `clone`; a `Cloneable`-but-not-default-constructible `T` fails with an opaque in-destructor error. Tighten/​document the concept. → **Fixed:** added two `static_assert`s in `Persist<T>` (`std::default_initializable<T>` and `std::assignable_from<T&, T>`) so a Cloneable-but-not-default-constructible/assignable `T` now fails with a clear message at instantiation instead of an opaque in-destructor error. Kept `Cloneable` itself narrow (clone-only) since it is reused where only the clone hook is the contract, and documented the extra `Persist` requirements in a `@note` on the concept.
15. ✅ core/concepts.h:130-139,185-189 — plain `FunctionRef` stored as a member silently dangles on a temporary; only `StoredFunctionRef` rejects rvalues, and nothing forces storing sites to use it. Enforcement is by convention only. → **Fixed (documented):** verified there is no live dangle — every current storing site (the `Timeline` tween members, animation.h) already uses `StoredFunctionRef`, and all plain-`FunctionRef`-alias uses are call-scoped parameters. Forcing storing sites via the type system would break the deliberate borrow ergonomics (temporaries must bind for parameter borrows), so the borrow-vs-store split stays a convention. Added a signpost comment at the alias-definition block stating these are borrow-only and that members kept across calls must be typed `StoredFunctionRef`, pointing at the canonical Timeline storing site.
16. ✅ core/sdf.h:422-431,628-637 — `Ring`/`DistortedRing` `get_vertical_bounds` duplicate a 16-line `clamp_phi` block verbatim that must stay in sync; factor into one helper. → **Fixed:** extracted the `center ± target_angle` latitude fold into a single `clamp_phi_band(center_phi, target_angle)` helper (returning a `PhiBand{phi_min, phi_max}`) next to `clamp_phi`. Both `get_vertical_bounds` now call it, so the fold policy is single-sourced. Pure refactor — identical arithmetic, no behavior change (sdf tests pass).
17. ✅ core/sdf.h:1782-1801 — `compute_azimuth_intervals` has no intermediate between tight per-row intervals and a full-width fallback, so a face covering ~0.6·2π azimuth scans the full width every row, undocumented relative to the ring/polygon paths. → **Fixed (documented):** expanded the `@details` to state that the test is binary on the single largest gap, so a face whose widest inter-vertex gap is ≤ π is treated as full-width even when its true coverage is well under 2π, and that this coarse over-cover (vs the ring/polygon paths) is a deliberate cost/precision trade on a hot bounds path — one sort + one pass, no multi-gap interval union. Behavior unchanged.
18. ✅ core/plot.h:500-519 — under planar edge interpolation, `v0`/`v1` registers are interpolated linearly in the *rendered* `t` while control-point register values are geodesic-arc-uniform, so arc-length/progress-driven shaders (gradients, dashes) drift nonlinearly mid-segment; only the absolute under-count is documented. → **Fixed (documented):** extended the inline comment to state the mismatch is also nonlinear within a segment (position advances by the planar arc, registers by the geodesic-chord lerp), so any shader using a register as a progress/arc-length proxy — gradients keyed on v1, dashes spaced in v0 — drifts against the drawn position mid-segment (worst where the planar arc bows farthest from its chord), while geodesic segments don't drift. Documentation only.
19. ❌ core/animation.h:1335 — `t_prev = t-1` is used as an unclamped path parameter; on a rewind it samples a non-periodic `ProceduralPath` outside `[0,1]`, producing an arbitrary span (compounds #1). → **Declined (wrong premise, same basis as #1):** the parameter is never outside `[0,1]`, so no clamp is needed. `Motion::step` calls `AnimationBase::step` (`t++`) as its first statement, and the repeat contract bounds `t`: `done()` is `t >= duration` and the Timeline rewinds (`t = 0`) or removes the animation *before* the next step, so the body only ever runs with `t ∈ [1, duration]`. Hence `t_prev/duration ∈ [0, (duration-1)/duration] ⊂ [0,1)` and `t/duration ∈ [1/duration, 1]` — both strictly in range, the lower seam giving an on-path `t_prev=0` (path start) and the upper giving exactly `1.0` (path end). The "unclamped" parameter is safe by the `done()`/`rewind` invariant, not by luck. No change (consistent with the #1 decline).
20. ✅ core/transformers.h:272-274,444-456 — `MobiusWarpCircular` shares `mobius_transform` with the non-circular variant but never returns to identity; on a non-repeating slot it freezes off-identity for the final composed frame (one-frame teardown discontinuity). The "stay warped" doc silently assumes a repeating slot. → **Fixed (documented):** added a `@warning` on `MobiusWarpCircularTransformer` stating it deliberately never returns to identity (the closed-loop `Animation::MobiusWarpCircular` holds the warp at full strength), so it is correct only in a repeating slot; in a non-repeating slot it freezes off-identity on the final frame (one-frame teardown discontinuity) and `MobiusWarpTransformer` should be used instead. Behavior unchanged — the "stay warped" property is intentional.
21. ✅ core/animation.h:597-619 — `ParticleSystem` applies the gravitational impulse to velocity, *then* multiplies by `friction`, so a just-added impulse is damped 15% the same frame, biasing orbits inward; physics is also implicitly `dt=1` (frame-rate dependent) with no system-level note. → **Fixed (physics + preset compensation):** reordered `step_particle` so `friction` drags the carried velocity BEFORE this frame's attractor impulse is added (forward Euler `v <- friction*v + impulse`); the fresh impulse is no longer damped the same frame, and a `@details` note documents the corrected order plus the implicit `dt=1` frame-rate dependence. Impulse magnitude now scales with attractor `strength` alone (decoupled from `friction`). Compensated the only affected effect — **MindSplatter** (the only ParticleSystem user with gravity attractors; FlowField uses `gravity=0`, so it is unchanged) — by pre-scaling each preset's `well_strength` by its friction (0.85): `1/1/1/2/3 → 0.85/0.85/0.85/1.7/2.55`. Since all MindSplatter presets use `friction=0.85`, this makes the per-frame pull identical to the old `friction*(v+impulse)` order, so the shipped look is preserved while the engine physics is now correct. Particle unit tests (removal/lifetime) unaffected; `test_effects.h` does not golden-hash.
22. ✅ core/platform.h:871-886 — host `EVERY_N_MILLIS` mock is a non-hygienic multi-statement macro that breaks in an unbraced `if`/`else`, diverging from the device's class-based FastLED macro (host/device parity gap). → **Fixed:** introduced a class-based `hs::EveryNMillis` throttle so the macro now expands to the same two-token `static EveryNMillis o(N); if (o)` shape as the device's FastLED `CEveryNMillis` macro, removing the extra non-static `__now` statement that was the structural divergence. Timing is preserved exactly (last_ starts at 0; trigger stamp never reset across switches — finding 23's concern is left intact; period captured at construction, matching the device). Verified by a syntax compile of braced/`if`-`else` usages and a runtime throttle check (2 fires over t={0,50,100,150,200,260}, period 100). Note this still cannot be an *unbraced* control-statement body — but that constraint now matches FastLED rather than being a host-only failure mode.
23. ❌ core/platform.h:872 — the host mock's function-local `static` throttle is never reset across effect switches, so the first post-switch tick timing differs from a fresh device boot. → **Declined (wrong premise):** the host throttle is a function-local `static` keyed to its expansion's `__LINE__`, so it persists for the whole program — but the device's FastLED `EVERY_N_MILLIS` expands to a function-local `static CEveryNMillis` that *equally* persists for the program's lifetime. Neither is reset on an effect switch; both are reinitialized only by a fresh boot. So host and device behave identically across a switch (both carry the prior throttle), and the premise that the host should match a *boot* on every switch is wrong — the device doesn't either. The one genuine host/device divergence — the host seeds its baseline to `0` while FastLED's `reset()` seeds to the current `millis()` — is finding #22's parity scope, and changing it would shift first-frame timing/goldens. No change.
24. ❌ effects/MeshFeedback.h:118 (with styles.h:189-247) — `presets.apply(style)` copies preset fields directly, bypassing the `registerParam` clamp; several presets set `frequency` far outside the registered `Distort Freq [0.01,1]` range, desyncing the GUI slider and snapping the value on first touch. The registered range is simply wrong for the values its presets drive. → **Declined (wrong premise — positional init mis-mapped):** the `Style` brace-init order is `{fade, hue_shift, amplitude, frequency, speed, scale}` (styles.h:90-97), so `frequency` is the *4th* field. Across all 8 presets its values are `0.21, 1.0, 0.42, 0.07, 0.01, 0.07, 0.014, 0.014` — every one inside the registered `Distort Freq [0.01,1]`. The out-of-range-looking `6.36`/`8.21` are the *3rd* field, `amplitude`, whose registered range is `Distort Amp [0,30]`; in range. Checked all 6 registered ranges against all 8 presets — every field is within range, so `presets.apply()` never snaps a value on first touch. No change.
25. ✅ effects/HopfFibration.h:226-229 — `tumble_speed` is multiplied into the fold displacement, so the "Folding" slider silently does nothing whenever Tumble Spd is 0 (undocumented cross-dependency). → **Fixed (decoupled, look preserved):** dropped the `params.tumble_speed` multiplicand from the fold term and rebased the constant `0.1f → 0.2f` so the default depth is unchanged at the default `tumble_speed=2.0` (0.1·2.0 = 0.2). Folding now acts independently of Tumble Spd — including a frozen tumble — with the cross-coupling documented at the site. (Non-default `tumble_speed` values no longer scale fold depth, per the decoupling.)
26. ✅ hardware/pov_sync.h:1561-1564 — the beacon "due" cadence (`beacon_period_revs`) and the rejoin budget are tuned independently with no `Config::valid()` clause tying them; a board joining in the gap waits up to ~`beacon_period_revs` revs (~2 s at 480 RPM) for identity, at the edge of the documented budget, asserted only in prose. → **Fixed (budget made a checked invariant):** added an explicit `rejoin_budget_revs` Config field (default 16 = the §9.1 ~2 s budget in revs at 480 RPM) and a `valid()` clause `beacon_period_revs <= rejoin_budget_revs`, so the boot-time `HS_CHECK` now ties the beacon cadence to the rejoin budget instead of leaving it to prose. Default config (`beacon_period_revs=16`) passes at the inclusive edge. Test: the existing `<32` resync-window boundary case now relaxes `rejoin_budget_revs` to isolate that bound, and a new sub-test pins the budget boundary (period 16 accepted, 17 rejected). Native suite green.
27. ❌ hardware/pov_sync.h:589-594,607-608 — `Flywheel::position`/`fold` cast the elapsed term to `int32_t`; the documented ±3.5 s safe window is ~the full int32 range, so a coast modestly longer than that (debugger halt, raised `revs_per_effect`) silently mis-paints with no trap. Add a saturation/`HS_CHECK`. → **Declined (maintainer decision):** the >±3.5 s coast is an abnormal, diagnostic-only condition (a debugger halt, or a `revs_per_effect` far above the shipping value) that does not arise in normal operation. `position()` is on the per-column render hot path, where the house discipline is a stripped `assert` backed by a cold trap, not an always-on `HS_CHECK`; adding a hot-path trap for a non-shipping condition is out of step with that rule. The int32 safe window is already documented at the call site (`position()`'s "±3.5 s" note). Left as-is; the documented window stands.
28. ✅ hardware/pov_segmented.h:666-675 (with pov_single.h:201-210) — the FastLED fallback render bodies are partly `#error`-disabled, partly dead, and use a different LED-index convention than the single-board map; delete the dead `#else` bodies or unify the conventions. → **Fixed (dead path deleted):** the top-of-file `#error` already makes `POVSegmented` fail to compile without `USE_DMA_LEDS`, so every non-DMA branch below it was unreachable. Removed the dead FastLED `#else` bodies in the constructor, `render_column`, and `render_black`; dropped the `#ifndef USE_DMA_LEDS` `leds_` buffer declaration and definition; unwrapped `overrun_count` to the DMA path and trimmed its stale "FastLED build" doc; the file is now cleanly DMA-only (no LED-index convention can diverge because there is no second path). `pov_single.h` is untouched — its FastLED/WS2801 path is live (no `#error`).
29. ✅ targets/wasm/wasm.cpp:613-635 — a misplaced/duplicated Doxygen block makes `getParameterDefinitions()` inherit `getParamGeneration()`'s doc; the two most order-sensitive JS calls are mislabeled in generated docs. → **Fixed:** moved the "Builds the GUI's parameter descriptor list" block down to immediately precede `getParameterDefinitions()`, so each function now sits under its own Doxygen comment (`getParamGeneration()`'s doc above `getParamGeneration()`, the descriptor-list doc above `getParameterDefinitions()`).
30. ✅ targets/wasm/wasm.cpp:1275-1300 — the `class_<MeshOps>` embind list is hand-enumerated with no compile-time guard that every `MESHOP_*`-generated operator is bound; a new operator compiles but is unreachable from JS (the one surface the codebase doesn't guard with an X-macro). → **Fixed (X-macro single source):** introduced `MESHOP_LIST(_OP0, _OP1F)` listing the 12 Conway/Goldberg operators once. It is expanded with `MESHOP_0`/`MESHOP_1F` to generate the wrapper methods and again with a new `MESHOP_BIND` to generate the embind `.function()` calls, so adding an operator to the list emits both the method and its JS binding — they can no longer drift. `relax`/`hankin` stay explicit (their signatures fit neither generator). Verified with a clean `wasm-debug` build (compiles + links).
31. ✅ daydream.js:282-300 — `export()` consumes `getParamValues()` without the detach/length guard `syncGUI()` applies to the same call; a heap-growth detachment on the click frame silently copies an all-zero preset to the clipboard. → **Fixed** (daydream repo, commit `38b38d3`): added the same `if (values.length === 0)` zero-length-view guard `syncGUI()` uses; on a detached view `export()` now surfaces a "Copy failed" status on the button and skips the copy instead of writing an all-zero preset.
32. ✅ driver.js:188-191 — the `PerspectiveCamera` is constructed with `canvas.width/height` before `setCanvasSize()` runs; if the container is 0×0 at construction the camera keeps a bogus/NaN aspect until a later resize (first-paint framing wrong). → **Fixed** (daydream repo, commit `ce47bae`): the constructor-time aspect now falls back to a square (`... || 1`) when `canvas.width/height` is 0/0, so the projection is always finite; `setCanvasSize()`/the ResizeObserver still install the real container aspect once it has a layout, as the code already documents.
33. ✅ driver.js:226-229 — `LineBasicMaterial({linewidth:5})` is a documented WebGL no-op; the axis lines never render thicker than 1px (dead/misleading config; also lissajous.html/splines.html). → **Fixed:** removed the `linewidth` property from all three `LineBasicMaterial` sites (driver.js axis lines, lissajous.html and splines.html curves) and left a one-line note at each that the WebGL renderer always draws 1px lines, so the value is dead config and must not be re-added.
34. ✅ segment_controller.js:686-690 — `composite()`/`tick()` set `frameComposited=true` from `pendingFrame` alone, so an all-null/fully-fenced frame marks a black buffer as a real frame to the recorder; derive the flag from whether ≥1 rect was actually blitted. → **Fixed:** `composite()` now returns the number of segment rectangles it actually blitted (boundary-marker decoration excluded), and `tick()` sets `frameComposited = blitted > 0` instead of unconditionally `true`. A fully-fenced frame composites nothing and is no longer captured as a real frame by the recorder.
35. ✅ segment_worker.js:74 (with controller readiness) — a WASM init failure that doesn't throw to global scope (404, non-throwing abort) leaves `ready=false` forever and `tick()` no-ops with no fault overlay — an invisible permanent freeze, the failure class the fault latch was built to prevent. Add a bounded init watchdog. → **Fixed:** `SegmentController.create()` now arms a 20 s one-shot watchdog; if every worker has not reported `ready` by the deadline it latches a pool-wide fault (segId -1) and paints the existing fault overlay instead of freezing black. Cleared once all workers are ready, and on fault/destroy. Scope is the init window only — the deliberate no-timeout policy on render faults (which do fire `onerror`) is unchanged. The timer is `unref()`'d so it can't hold a Node test process open. The overlay header now renders a negative segId as "pool init".
36. ✅ segment_layout.js:23-59 — the JS tiler is hand-mirrored from `pov_segment_map.h` with no shared source of truth and diverges for N>4 (which the C++ map rejects); a C++ edit won't be caught here (no device build in CI). → **Fixed (documented):** the "divergence for N>4" is intentional, not a latent bug — the GUI exposes 2..8 segments and N in {6,8} is simulator-only extra-Y-band parallelism with no device counterpart (the C++ map rejects N>4). Constraining the JS to the C++ ≤4 domain would break the working 6/8-worker simulator modes. Expanded the `computeSegmentRange` doc to state it is the simulator's render tiler (not a 1:1 port), that the N in {2,4} regime corresponds to the firmware map and is pinned by `segment_crosscheck.test.js` against the C++ host-test fixture, and that N in {6,8} intentionally has no firmware counterpart. (The residual no-codegen / no-device-CI source-of-truth gap is a documented project-wide limitation; see finding 44 for strengthening the cross-check.)
37. ✅ segment_worker.js:165 (with worker_protocol.js:91-97) — `FrameMsg.pixels` typedef is non-nullable but the controller treats it as nullable (`?? null`); the dead-defensive path contradicts the protocol "single source of truth." → **Fixed:** the worker always allocates a fresh pixel buffer per render and `FrameMsg.pixels` is typed non-nullable, so the controller's `?? null` was dead code contradicting the protocol. Removed it (read `msg.pixels` directly) and noted at the site that the protocol is the source of truth.
38. ✅ tools/palettes.html:1009-1034,1206 — the `requestAnimationFrame` loop is never cancelled and redraws the marker every frame even at `animationSpeed===0`; the `animationFrameId===null` guard is dead. → **Fixed (redraw):** `animate()` now returns early (skipping the swatch/strip redraw) when `animationSpeed === 0` and no drag is in progress, so a parked marker no longer repaints an identical frame ~60×/s; the loop resumes drawing the moment speed becomes nonzero or a drag begins, and discrete interactions already redraw directly. The loop's full cancellation/teardown is the broader tool-page dispose gap tracked by finding 133.
39. ✅ tools/splines.html:363-379 — dragging a control point rebuilds both line geometries and re-runs full WASM spline sampling on every `mousemove`, with no rAF coalescing (solids.html solved exactly this with `scheduleUpdate`). → **Fixed:** added a `scheduleUpdateCurves()` rAF coalescer (mirroring solids.html's `scheduleUpdate`) and switched the drag handler to it, so the geometry rebuild + WASM sampling runs at most once per frame. The dragged dot's position still updates immediately for responsiveness.
40. ✅ tools/solids.html:946-959,1607-1615 — the string-keyed edge-extraction block is duplicated verbatim between `generateThumbnails` and `renderMesh` and allocates a template string per half-edge. → **Fixed:** extracted a `uniqueEdges(faces, vertexCount)` helper returning `[lo, hi]` index pairs, keyed numerically (`lo*N + hi`) so the per-half-edge `${a}_${b}` template string is gone, and routed both `renderMesh` and `generateThumbnails` (and `saveSolid`, finding 41) through it.
41. ✅ tools/solids.html:1054-1078 — `saveSolid` reads `eCount` from `currentMesh.edgeCount`, set only on the `renderMesh` success path; after a failed op chain it silently saves 0, and `iCount` is stored but never displayed. → **Fixed:** `saveSolid` now derives the edge count directly from the faces via `uniqueEdges()` instead of the success-path-only `currentMesh.edgeCount`, so a save after a failed op chain reports the real count, and the previously-stored-but-unshown `iCount` (face-vertex incidences) is appended to the saved card's stats string.
42. ✅ tools/solids.html:763-775 — `updateArenaMetrics()` self-loops via `setTimeout(…,500)` forever with no clear handle (perpetual timer). → **Fixed:** the self-rescheduling poll now stores its timeout handle and a `pagehide` listener clears it, so the 2 Hz loop is cancellable instead of perpetual.
43. ✅ tools/mobius.html:638-651 — a 128×128 `SphereGeometry` (~33k verts) for a fragment-shaded sphere, plus a second wireframe sphere added with no reference kept and no dispose path. → **Fixed:** dropped the geometry to 64×64 (the vertex shader is pass-through and all the detail is per-fragment, so ~4× the tessellation was wasted), kept a `wireMesh` reference to the previously anonymous wireframe sphere, and added a `pagehide` handler that disposes both spheres' geometry/material.
44. ✅ daydream/tests/segment_crosscheck.test.js:45-74 — the "firmware equivalence" check compares `computeSegmentRange` to a hand-written JS reimplementation of the C++, not to compiled C++ output; the JS port can drift and the test stays green. → **Fixed (as far as feasible without compiled C++ in CI):** running compiled C++ in the Node test isn't possible (no device build in CI), so the drift risk is closed by independent goldens instead. The canonical N=4/S=288 fixture already pinned the `cppSegmentMap` port to hardcoded `{armB,yBase,yStep}` values mirroring the C++ host-test fixture; added a "pov_segment_map port matches independent goldens" test that does the same for *every* swept config (N=2/S=288 and N=4/S=8), so the port can no longer drift in lockstep with `computeSegmentRange` and stay green — a port convention change now trips a literal golden regardless. New test passes (3/3 in the file).
45. ✅ daydream/tests — no tests for `driver.js` (the zero-copy normalized `instanceColor` binding / 65535→1 GPU contract) or `daydream.js` orchestration (subscribe→apply glue, the fight-the-slider param poll, segment-mode interleave); green CI does not imply a working app. → **Addressed via the codebase's extract-and-test pattern (daydream repo, commit `2bec445`):** the project deliberately pulls every DOM-free seam out of the `driver.js`/`daydream.js` shells into helper modules that *are* unit-tested, so the contracts this finding names are largely already covered — the zero-copy view/detach contract is literally the purpose of `pixel_view.js` (its doc: "Both the driver … and daydream.js … route their 'is this view still live?' decision through here so the buffer-alias contract lives in one tested place"; `pixel_view.test.js`); subscribe→apply/URL glue is `state.js` (`state.test.js`); segment-mode interleave is `segment_layout.js` (`segment_layout.test.js`, `segment_crosscheck.test.js`). The one named piece still inline was the "fight-the-slider" param poll: extracted its per-controller decision (bool-coerce / skip-while-editing / skip-if-unchanged) into a DOM-free `resolveParamSync` (`param_sync.js`), routed `syncGUI()` through it, and covered the numeric/boolean/editing paths in `param_sync.test.js`. The remainder is genuinely un-unit-testable: the `65535→1 instanceColor` mapping is a WebGL `normalized` attribute *flag* (no JS to test), and the residual THREE.js/lil-gui/DOM wiring in the two shells needs a browser/E2E harness, not a unit test.
46. ✅ daydream/tests/engine_contract_wasm.test.js — pins the FakeEngine method *surface* but not return-value semantics (e.g. `setEffect`/`setParameter` → `bool`, nonzero arena capacities), so the FakeEngine could model these as void undetected. → **Fixed (in the daydream repo, commit `ab41a1c`):** the real-engine contract test now asserts `setEffect` and `setParameter` each return a strict `boolean` (`true` for a registered effect / known param fed its own value), matching the `=== false` gate `daydream.js` and the GUI rely on, and that every arena reports `capacity > 0` with `usage` within it — not merely numeric fields. A void-returning or zero-capacity FakeEngine now diverges from the pinned contract.
47. ✅ tests/test_transformers.h:163,279 — `Ripple`/`Noise` have zero-amplitude identity tests but no companion asserting non-zero amplitude moves a point by a known magnitude; a globally no-op transformer passes. → **Fixed (noise side):** `Ripple` was already covered — `test_ripple_active_rotates_on_sphere` asserts the point moves by `>1e-2`. The gap was real only for `Noise`: `test_noise_active_stays_on_sphere` checked finite + on-sphere but not displacement, so a no-op `noise_transform` would have passed it. Added a summed-displacement `HS_EXPECT_GT(total_moved, 1e-2f)` (summed across samples to avoid flaking on a single noise zero-crossing). A "known magnitude" exact-match oracle is impractical for the procedural noise field; the displacement lower bound matches the established ripple pattern.
48. ✅ tests/test_mesh.h — `pairs_are_symmetric` checks `pair(pair(i))==i` but not that paired half-edges are geometrically opposite (same undirected edge, reversed endpoints); a reciprocal-but-wrong pairing passes. → **Fixed:** `test_half_edge_mesh_pairs_are_symmetric` now also asserts the twin traverses the same undirected edge in reverse — for `u→v`, `head(pair)==tail(i)` and `tail(pair)==head(i)` (tail derived from `prev`'s head). A pairing that is reciprocal but joins the wrong edges now fails.
49. ✅ tests/test_conway.h,test_solids.h — `check_euler` re-derives V/E/F from the mesh under test, so a class-wide omission/duplication preserving V−E+F=2 is invisible for Conway outputs and non-Platonic solids (no independent expected-count oracle). → **Fixed (at the foundational case):** `test_euler_platonic_solids` now passes a fixed `(V, E, F)` oracle for each of the five Platonic solids (`{4,6,4}` tet … `{12,30,20}` icosa — invariant mathematical constants), and `check_euler_for_index` asserts each independently on top of `V−E+F==2`. A builder bug that uniformly miscounts but keeps the relation is now caught at the base solids. Conway *outputs* already carry independent count oracles on the cube seed (`test_dual/kis/ambo/truncate/expand/chamfer_cube` assert exact V/F). Exhaustive oracles for every Archimedean/Catalan/Conway combination were deliberately *not* added: those generators can weld near-coincident vertices, so a hardcoded canonical count can diverge from a still-correct mesh — the risk outweighs the marginal coverage, and the spherical-validity sweep already guards them structurally.
50. ❌ tests/stack_measure.cpp,perf_bench.cpp — stack budget is a host x86-64 estimate with hand-waved ISR allowance and no device validation; `perf_bench` is not a CTest, so there is no automated render-regression gate at all. → **Declined (both halves are intentional/inherent):** *(a) Stack budget.* No device build runs anywhere automated (no CI runner has Teensy hardware), so on-device stack validation is impossible to add — it is a project constraint, not a test-design defect. The host estimate is deliberately *conservative*: it runs the real workload via stack-painting at the device 288×144 resolution with `-Os` + the device math flags, and x86-64's 8-byte pointers make every frame *larger* than the device's 4-byte-pointer frames, so the host peak over-estimates the device. The ISR allowance is documented (~104 B FPU exception frame + locals per nested flywheel/DMA/sync ISR) and the budget sits below the device's 16 KB `ram1` reservation to absorb it. *(b) perf_bench not a CTest.* This is deliberate and documented (`tests/CMakeLists.txt`: "Not CTests — wall time is noisy"). Host wall time is a *relative* proxy for device frame time (±10–15% run-to-run on shared CI runners), so a threshold-gated CTest would flake; `perf_bench` is an informational tool for local `-Os` vs `-O3` comparison and per-effect regression spotting, not a gate. Converting it to a gating CTest would trade a real false-positive problem for the appearance of coverage. No change.
51. ✅ tests/test_param_marshal.h — verifies within-effect index alignment but not the C++-roster-to-JS-GUI ordinal binding; reordering `HS_EFFECT_LIST` would silently re-bind every GUI slider and pass. → **Fixed (with a corrected premise):** the GUI binds sliders by parameter *name*, not by effect ordinal, so a reorder does not mis-bind a slider — but it *does* silently shift the effect ordinal the WASM factory enumerates and the JS effect-list surfaces, and nothing caught that (the startup check and this test both only pinned the roster *count*). Added `check_roster_order_pinned()`: an independent hand-maintained golden roster compared element-by-element against the X-macro expansion of `HS_EFFECT_LIST`. Any reorder/insertion/removal now fails until the golden list is updated on purpose — a deliberate, reviewable diff, matching the codebase's X-macro freeze discipline.
52. ✅ tests/test_animation.h — `Timeline` cancel/double-add/zero-active paths are count-only asserted; repeating-`Motion` drift is sampled at only 3 phases, coarse enough to miss slow monotone drift. → **Fixed:** (1) `test_timeline_cancel_removes_repeating_animation` now also proves *behavior* — after the cancel+removal it steps six more frames and asserts the target stays frozen, so a "removed" event that was actually a replaying zombie (oscillating the target each cycle) fails. (2) `test_timeline_full_guard_rejects_overflow` gives the overflow event its own target and steps once, asserting it never advances — proving the rejected add is truly absent, not silently queued. (3) `test_motion_repeating_does_not_drift` now captures the final cycle's head at *every* within-cycle phase and checks the full set of rotation-invariant internal angles against the ideal (anchor-to-all over phases 1..duration−1), instead of three hand-picked probes — a slow monotone drift can no longer hide between coarse samples, while precession-immunity is preserved.

### Low

53. ✅ core/3dmath.h:1073-1086 — `make_rotation(from,to)` snaps near-parallel inputs to identity, silently dropping sub-0.8° rotations; doc note warranted. → **Fixed:** added a `@details` note to the `make_rotation(from,to)` overload spelling out the near-parallel snap-to-identity (d > 1 - TOLERANCE ⇒ angle < ~0.81°, so sub-0.81° rotations are silently dropped) and the antiparallel π-rotation path, with the caveat that callers needing exact tiny rotations must not route them through this overload. Doc-only; no behavior change.
54. ✅ core/3dmath.h:1140 — `quaternion_from_basis` traps (via `normalized()`) on a near-singular basis, contradicting its "silently yields a non-rotation" doc. → **Fixed:** reconciled the doc to distinguish the two regimes — a mildly skewed (non-degenerate) basis silently yields a non-rotation (the trailing `normalized()` still succeeds), while a near-singular basis drives the Shepperd divisor and `q` toward zero magnitude so `normalized()` traps (fail-fast). Doc-only; no behavior change.
55. ✅ core/3dmath.h:323 — `fast_atan2`'s additive `1e-10` epsilon is a precision cliff for legitimately tiny `y` (order-1 relative error). → **Fixed (doc, with corrected premise):** the additive bias does drive the *relative* error toward order-1 for |y| ≲ 1e-10, but the *absolute* angle error stays far below the ~0.0038 rad Hastings polynomial error, and every caller (hue/azimuth lookups) consumes the angle absolutely — so it is harmless here. Replaced the over-claiming "never perturbs a non-degenerate result" comment with an honest note of the relative-vs-absolute behavior and the scale-up workaround a relative-accuracy caller would need. The additive form is kept: it is branchless on the hot path and the only alternative (a 0/0 guard branch) would cost a per-call compare for no caller-visible gain.
56. ✅ core/3dmath.h:1218 / geometry.h:539,785 — redundant `.normalized()` after unit-by-construction builds (`fib_spiral`, `lissajous`); `logPolarToVector` proves-and-omits — apply consistently. → **Fixed:** dropped the trailing `.normalized()` from `fib_spiral` and `lissajous` (both are unit by construction: `sin²φ(cos²+sin²) + cos²φ = 1`), adding the proves-it comment in the style `logPolarToVector` already uses. The unit-length tests (`test_fib_spiral_unit_length`, lissajous suite) pass with their 1e-3 tolerance, confirming the build is unit to float precision without the renormalize. (The `3dmath.h:1218` reference in the finding is the `slerp` normalize, which is genuinely required — its `s1·v1 + s2·v2` combination is *not* unit by construction — so it is left in place.)
57. ✅ core/geometry.h:481 — pole round-trip `vector→pixel` can yield `y > H_VIRT-1` from `fast_acos` error; undocumented at this end. → **Fixed (doc):** added the `vector→pixel` counterpart of the unchecked-`y` contract that `pixel_to_vector` already documents. At the south pole `fast_acos` returns `PI_F` and `phi_to_y` evaluates `(PI_F*(H_VIRT-1))/PI_F`, whose float multiply-then-divide need not round back to exactly `H_VIRT-1`, so `y` can sit a hair above the top row. Left unclamped on the same per-pixel hot-path grounds as the inverse end; the `@return` note now tells a caller that indexes a row buffer with `(int)y` to clamp/floor first. Doc-only.
58. ✅ core/geometry.h:67,517-520 — bare magic tolerances/sentinels (`0.0001f`, `0.00001f`, `±10.0f`) instead of the centralized `math::EPS_*`/`STEREO_*` constants. → **Fixed (value-identical):** `fragment_edge_dist`'s `0.0001f` face-size guard now uses `math::TOLERANCE` (== 1e-4); `vectorToLogPolar`'s two `0.00001f` pole guards now use `math::EPS_GEOMETRIC` (== 1e-5); and the `±10.0f` rho sentinels became a named `LOGPOLAR_RHO_SENTINEL` constant (with doc) referenced as `±LOGPOLAR_RHO_SENTINEL`. All three substitutions are bit-identical to the literals they replace (no behavior change — the pole-sentinel tests at `±10.0f` still pass). The `STEREO_*` family was deliberately *not* reused: it is the stereographic-projection sentinel scale (1e4), a different quantity from the log-polar rho clamp.
59. core/geometry.h:142-154 — canonical axis constants live in geometry.h, split from `Vector` in 3dmath.h.
60. ✅ core/util.h:61 / waves.h:45,73 — `wrap` template and `wrap_t` duplicate the boundary policy; `tri_wave`/`square_wave` use the heavier `fmod` `wrap` on a per-sample path.
61. ✅ core/easing.h:98,131 — `ease_out_expo`/`ease_out_elastic` have no lower guard for `t<0`, returning wildly out-of-range values (siblings guard their radicand).
62. ✅ core/color.h:528 — `CPixel(uint32_t hex)` is non-explicit (the one hole in an otherwise explicit-cast policy); a stray int silently becomes a color.
63. ✅ core/color.h:1116 — golden-ratio hue step truncates (157) instead of rounding (158); comment inaccurately says "rounded." (Fixed by correcting the comment: 158 is even, not coprime with 256, so it would short-cycle — 157 truncation is load-bearing and must stay; only the wording was wrong.)
64. core/color.h:976-1023 — `Gradient` stop positions are floor-quantized `(int)(pos*255)`, drifting authored centroids up to ~1/255 below intent (deviates from the file's round-to-nearest discipline).
65. core/color.h:1404 vs 1259-1311 — chroma envelope uses `fast_sinf` on sampling but exact `sinf` on authoring, so a stop's reproduced chroma isn't bit-identical to its authored value.
66. core/color.h:838-862 — `lerp_oklch` achromatic-start blend adopts the target hue instantly at `t≈0⁺`; correct but undocumented.
67. core/color.h:1242 / styles.h:189-247 — tuning constants (`kChromaPeak`, `kHueTorsion`, `0.12/0.55` L-band) and positional 8-field preset brace-init have no compile-time anchor; a reorder or LUT change silently mis-tunes.
68. ✅ core/memory.h:243 — `TriangularBitset::index` (an OOB-*write* primitive) is guarded by stripped `assert`, not `HS_CHECK`, unlike every sibling write primitive; on device an out-of-range pair writes adjacent memory silently.
69. ✅ core/memory.h:83-100,529 — over-aligned (`>max_align_t`) allocation requests and null-`data_` `append_bulk` are safe only by latent invariant, not by an explicit guard. → **Fixed (documented the invariants):** the over-align case was already correct — padding derives from the true address, so any power-of-two `align` is honored regardless of the buffer's base alignment; added a note making that explicit at the power-of-two guard. For `append_bulk`, the genuine subtlety is that a zero-size `bind()` leaves `data_ == nullptr` with `capacity_ == 0`, so the capacity guard (`count <= capacity_ - size_ == 0`) forces `count == 0` and the existing empty-append early-return fires before the memcpy — the non-null destination is therefore *enforced*, not assumed. Documented that chain at the early-return. No behavior change.
70. ✅ core/concepts.h:149-150 — a null function pointer passed to the `Ret(*)(Args...)` ctor isn't caught (the empty-check only covers default construction). → **Fixed:** the function-pointer ctor now returns early on a null `func`, leaving `thunk_` null so the ref is *empty* — `operator bool` reads false and the operator() null-check catches the misuse, matching default construction. Previously a null `func` installed a non-null thunk that dereferenced null on the first call. The branch is on the cold construction path, so the per-call hot path is unchanged.
71. ✅ core/inplace_function.h:107,193 — the empty/moved-from `storage_` is never zero-initialized; harmless today but a latent footgun if a future op touches the empty buffer. → **Fixed:** the default and `nullptr` ctors now value-initialize `storage_` to zero (`: storage_{}`), so an empty function's buffer holds defined bytes. The value-carrying ctors (converting/copy/move) deliberately keep `storage_` out of their init list — they placement-new/copy into it, so zeroing first would waste work on the construction hot path; this keeps the fix off the perf-sensitive path. A moved-from buffer still holds the moved-from object (defined, not garbage), so it needs no zeroing.
72. ✅ core/sdf.h:153 — dead `struct Interval` (never referenced; its `[start,end)` doc invites a wrong assumption). → **Fixed:** deleted the struct. A repo-wide grep confirmed the type is referenced nowhere — every remaining "Interval" mention is prose/`@param` text or the unrelated `pair<float,float>` interval lists; the live sibling `struct Bounds` is untouched.
73. core/sdf.h:768-798 — the `Union`/`SmoothUnion` overflow comment claims the trap "cannot fire," but nested CSG children can exceed `2×kIntervalSpanCap` and *do* trap (fail-fast, not silent) — reword.
74. core/sdf.h:225-229,2841 — `Line::get_horizontal_intervals` reimplements `emit_cap_interval`'s 4-line cap guard inline instead of calling it (copy-paste); the magic boolean return polarity is non-obvious.
75. core/sdf.h:1908-1998 — `compute_full_bounds` writes `thetas[i]` for a degenerate edge but skips its plane, decoupling the compacted `planes[]` index from per-edge data (latent footgun).
76. core/sdf.h:2436-2438,1668 — `Star`'s `len` divisor is guarded indirectly via `radius>0`; `compute_inradius`'s `1e8f` degenerate branch is effectively dead.
77. core/scan.h:251-259,337 — `scan_region`'s triple sequential clamp and `BoundingSphere`'s W/2-clamp→full-row two-step are correct but hard to verify by inspection / rest on an undocumented cross-file contract.
78. core/plot.h:447-471 — adaptive-substep overshoot biases the normalized replay slightly; a dead `else` branch is kept with a paragraph explaining it is dead (use `HS_CHECK`).
79. ✅ core/plot.h:548 — near-pole row-cull margin epsilon is added on the planar branch but not the geodesic one (undocumented asymmetry). → **Fixed**: documented in `edge_row_span`'s geodesic branch why it carries no one-row epsilon — its span is the exact closed-form y range (endpoint y values are the rendered endpoints, `y_to_row` is monotonic), with no project/unproject round-trip to leave the cull short, unlike the planar branch.
80. core/filter.h:533-535,785,1168 — `OrientSlice` comment overstates a clamp-eliminated hazard; World vs Screen `Trails` use different (undocumented) TTL rounding; fractional-`ttl` fade-in is one-frame asymmetric.
81. core/filter.h:1402-1405 / plot.h register docs — a comment cites a stale line number (`:1378`), violating the "self-describing comments" convention; per-primitive "Registers:" doc blocks are unevenly present.
82. core/animation.h:704,2197 — a dead particle holds its pool slot for up to `TRAIL_LEN` frames (capacity coupling); a finished `Ripple` keeps advancing `phase` past end (dead work).
83. core/animation.h:1631,1688,1406 — the "least-parallel body axis" primitive is spelled three different ways (`Motion::path_frame`, `RandomWalk` ctor and step); extract one helper.
84. core/animation.h:1737 vs 859 — `ColorWipe` uses a fragile `t==0` start-capture while `Transition` uses a robust `captured` flag; standardize on the flag.
85. core/animation.h:471 — `Particle::init` clamps life to `min(l,65535)` but not `max(.,0)`; a negative `l` narrows to a huge `uint16_t` (the system caller is safe, the API isn't).
86. core/animation.h:2406 / transformers.h:360 — the "pin the largest type" `static_assert` covers only one type, not the actual largest; noise decorrelation offsets (100/200) are uncentralized magic constants.
87. core/platform.h:989-993 — `random_to_unit` truncates `hs::random().max()` to `uint32_t`, correct only for the current mt19937; a future RNG swap silently breaks the `[0,1)` guarantee.
88. core/platform.h:1043-1067 — `check_fail` formats into a 96-byte buffer before the 256-byte sink, truncating long HS_CHECK messages despite the "no truncation" rationale.
89. core/platform.h:1310,1093 — `CycleCounter`/`active_` statics lack the reentrancy note `hs::random()` carries; `g_scan_metrics` is an inline global even when metrics are disabled.
90. core/canvas.h:651-655,92 — `prev()` returns by value while `operator()` returns by reference (undocumented asymmetry); "WASM-only" `render_us` members are compiled (dead) on device.
91. core/constants.h:36-57 — `render_y_start/end` clamp only one side each (no `y_start<=y_end` guard), and the `render_x` margin trap (`margin<w`) is weaker than the documented `margin<=x_start+w` contract.
92. core/effect_registry.h:156-159 / effects.h — the `retain` attribute has no `__has_attribute` fallback; the `#include` list and `HS_EFFECT_LIST` are two hand-maintained parallel lists.
93. core/led.h:21 — commented-out `USE_DMA_LEDS` toggle left in an engine-wide header (foot-gun).
94. effects/BZReactionDiffusion.h:355 — comment cites "finding 396," violating the project's no-finding-refs-in-comments rule; scrub the parenthetical.
95. effects/BZReactionDiffusion.h:308 — dead defensive `hs::clamp` on a convex combination that can't exceed range.
96. effects/Comets.h:270-281 — 12-entry Lissajous table is unlabeled magic-number data.
97. effects/Dynamo.h:50-129 / FlowField.h:58,93 — duplicated palette-construction args; bare positional magic literals for friction/gravity/max_life and noise offsets (against the file's own naming discipline).
98. effects/GnomonicStars.h:71-74 — uses `UP` where peers use `Y_AXIS`; binds "Warp Speed" to transformer-slot storage rather than the `Params` struct (the only effect whose control isn't in `Params`).
99. effects/Liquid2D.h:262-270 — `Params::lerp` aliases the first 7 floats by a hand-maintained pointer array with no `offsetof`/`static_assert` guard; a field reorder silently mis-lerps. (Three different per-`Params` lerp idioms exist across effects.)
100. effects/MindSplatter.h:228-296 — redundant per-spawn `make_basis`; `cosf/sinf` where the house style is `fast_*`; a bare `assert` (vs `HS_CHECK`) on a float-derived pool index.
101. effects/Moire.h:62 / IslamicStars.h:44 — dead struct-default overwritten in `init()`; `Fade` slider's upper half is inert at low `Duration` (clamp `min(fade,dur/2)`).
102. effects/MobiusGrid.h:99-104 — undocumented identity-quaternion fallback on a degenerate midpoint (unlike sibling `normalized_or` guards).
103. effects/SphericalHarmonics.h:344,239 — redundant `static_cast<int>` on an int; dead `next_idx=6` store overwritten before use.
104. effects/PetalFlow.h:130,236 — undocumented magic `-0.01f` ring nudge; `+1` dead capacity in a `fragments.bind`.
105. effects/RingShower.h:163 — the `life>0` invariant (which `radius_at` divides by) rests entirely on an unnamed `+8.0f` floor.
106. effects/ReactionDiffusionBase.h — the three mandatory init calls are re-listed per derived class; omitting one compiles and silently produces a static view. Provide a single `init_lattice()`.
107. effects/Thrusters.h:265 — `kDoubledCycleDivisor=PI_F` is a deliberate 2× frequency trick that reads like a `2*PI_F` typo (well-commented, flagged so it isn't "fixed").
108. (effects, README) — FlowField registers five params the README lists none of (code/README drift).
109. hardware/hd107s_frame.h:69 — `END_FRAME_BYTES=(N+15)/16` over-latches by up to ~7 clocks at small N; the stated `ceil(N/2)→ceil(N/16)` derivation is imprecise (matches only by double-ceil coincidence).
110. hardware/pov_sync.h:1611 — `build_word_` is the lone surviving `volatile` cross-context handoff after the deliberate `volatile→atomic` migration; use `std::atomic<uint32_t>` relaxed for consistency.
111. hardware/dma_led.h:114-122 — `transferStartUs_` is stamped *after* `transferComplete_.store(false)`, so the completion ISR firing between them leaves a stale stamp for a completed transfer; stamp before clearing the flag.
112. hardware/pov_sync.h:82-87 — `circ_dist`'s single `+= w` wrap-fix is correct only while callers keep inputs in `[0,W)`; the safety rests on caller discipline.
113. targets/wasm/wasm.cpp:1087 — `snub` is bound as a 0-arg operator, so its `t`/`twist` controls are unreachable from JS (mis-bucketed).
114. targets/wasm/wasm.cpp:596,484 — `setParameter` delegates finiteness validation one layer down (inconsistent with the file's reject-at-boundary pattern); the `*3` channel count is a bare literal repeated ~6×.
115. CMakeLists.txt:79-82 — `-ffast-math -fno-finite-math-only` are compile-only while LTO defers codegen to link; correct today but the asymmetry is undocumented (a link-time math pass could silently re-enable finite-math).
116. ✅ justfile — no recipe invokes `wasm_smoke.mjs`/`capture_screenshots.mjs`; the stated regression gates are CI-only, so `just build` ships an un-smoke-tested module. → **Fixed:** added a `smoke` recipe (`build` dependency + `node scripts/wasm_smoke.mjs`) so the WASM module's runtime gate — the same one the CI `wasm` job runs — is one command away locally, and a `screenshots` recipe wrapping `npm run screenshots`. `just build` still only builds, but the smoke gate is no longer CI-exclusive.
117. ✅ scripts/generate_luts.py:62,84 — `65536`/`256` sizes duplicated across docstring/`range`/C decl; `per_row` magic numbers unexplained. → **Fixed:** introduced named `SRGB_LEVELS`/`LINEAR_LEVELS` (256/65536) and derived `SRGB_MAX`/`LINEAR_MAX` (255/65535), routing the `range()` counts, the `i/<max>` input normalization, the `quantize()` output ceiling, and the emitted C array bounds (via f-strings) through them so each size lives in one place. Documented that the `per_row` counts (11/15) only shape the unformatted fallback layout — clang-format repacks every row to the column limit when present, so they never reach committed output. Verified the generated `core/color_luts.h` is byte-identical before/after.
118. scripts/wasm_smoke.mjs:240 — `relax(1e9)` proves the C++-side clamp but not embind's int coercion near INT32 overflow (the test asserts less than its comment claims).
119. targets/Phantasm/Phantasm.ino:78 — `Serial.begin(9600)` is dead config on Teensy USB-CDC (baud ignored); reads as meaningful when it isn't.
120. daydream.js:656-672 — Test-All `setInterval` can call `applyEffect` during the async WASM-load window with `wasmEngine===null` (effect silently doesn't apply).
121. driver.js:757 / daydream.js:455 — after a resolution change `Daydream.pixels` transiently aliases a fresh JS buffer (not WASM memory) until the next refetch; healed each frame but undocumented.
122. gui.js:264,312 — `applyOnLoad` is true even when a URL enum value was rejected as out-of-range, firing a spurious onChange/URL write with the default.
123. recorder.js:155,347 — bitrate change mid-recording is silently deferred with no UI feedback; `showSaveFilePicker` is referenced as a bare global rather than `window.showSaveFilePicker`.
124. daydream.js:160 / state.js:239 / driver.js:697 — `syncGUI`'s `getValue()!==val` re-triggers `updateDisplay` every frame on a NaN engine value; `URLSearchParams.set` would `String()` a numeric tracked key without `roundUrlNumber`; LOD curve constants (`30/30000/3`) are un-named.
125. index.html:23-67 / gui.js:387 — large duplicated inline-style blocks despite `styles/index.css`; triple symbol export plus an effectively-dead `setUrlParam` writer.
126. segment_controller.js:204-241,500 — `msg.segId` indexes `results`/`timings` without a bounds check (safe today via handler-nulling); a second worker fault is silently coalesced; the boundary overlay (default on) is baked into recorded video.
127. segment_worker.js:160 — `renderUs` (engine internal) and `elapsed` (JS `drawFrame` wall) measure overlapping-but-different spans surfaced as separate columns with no doc.
128. tools/slider.js:58-83 — `createSlider` validates `step>0` in display space but `Math.round(step*scale)` can floor a fractional step to a browser-broken `step="0"` with no diagnostic.
129. tools/spline_math.js:37-79 — `generateBezierCurve`/`generateCatmullRomCurve` divide by sample counts with no zero-guard (produces NaN points); `randomPointOnSphere` wraps an already-unit vector in a redundant `vec3Normalize`.
130. tools/palette_math.js:332-349,118 — the tool reconstructs from the bridge's 8-bit sRGB LUT vs the engine's 16-bit linear `BakedPalette`, so previews can exceed the "~1 LSB" claimed parity; `PRNG` falls back to `Math.random()` on a falsy seed (non-reproducible).
131. tools/solid_codegen.js:53-108 — `o.params.iter||100` silently coerces an explicit `iter:0`; `funcName` collisions (quantized suffixes) silently overwrite earlier solids in a "byte-stable" generator.
132. tools (cross-module) — five independent C++ float-literal formatters, four sharing one idiom and `proceduralPaletteCpp`'s diverging (no trim/sci-notation guard); centralize in `shared.js`.
133. tools/*.html — `initScene`'s `dispose()` is never called (RAF + window/document/canvas listeners leak on teardown); every page uses `window.onload=init`, clobbering other handlers and swallowing sync init errors (lissajous/mobius have no top-level guard).
134. tools/solids.html:445-1123 — heavy inline `onclick=`/`oninput=` HTML-string attributes force handlers onto `window.*` and rebuild handler-bearing strings per render (inconsistent with the other four pages' `addEventListener`).
135. tools/solids.html:1663-1715 — `showIndices` creates up to ~1000 `<div>`s (no `DocumentFragment` batching) and re-projects every label each frame in `onAfterRender`.
136. tools/palettes.html:654,968-984 — `getNormalizedX`'s `clientX || touches[...]` is fragile at `clientX===0` with a dead touch branch (no touch listeners registered); overlay `mapValue` magic constants recomputed inline.
137. tools/lissajous.html:170 / mobius.html:336-380 / solids.html — redundant `parseInt` on a number; Möbius shader `cdiv` returns 0 / `(100,100)` sentinels for degenerate transforms with no user feedback; `JSON.parse(JSON.stringify())` deep-copy where `structuredClone` fits.
138. tools/*.html, tools.css — duplicated `.btn` variants, hand-rolled toggle-switch markup (6×), identical vendor `<head>` block across all five pages, and a duplicated `COPY_FEEDBACK` literal; absorb into `tools.css`/`shared.js`.
139. tools (a11y) — custom toggles lack `role="switch"`/`aria-pressed`; the Möbius complex-plane drag pad and palette canvas-zoom are mouse/touch-only with no keyboard or ARIA.
140. tests/test_geometry.h:380 — `random_vector` asserts only unit length, never distribution or determinism (a degenerate/biased RNG ships green); RNG-touching tests bifurcate (some seed-and-assert, some don't).
141. tests/test_transformers.h — `Ripple` decay parameter never exercised (all tests max-strength); threshold reject-band tested off-boundary only; no NaN/Inf into the transformer wrappers.
142. tests/test_mesh.h — `clone_meshstate_deep_copies` skips the element-wise comparison on a size mismatch instead of failing; `face_loop_closes` uses an arbitrary `>100` break without asserting the expected loop length.
143. tests/test_animation.h — `ParticleSystem` capacity test never `step()`s or verifies spawned-particle init; attractor kill tested only at distance 0 (no `==kill_radius` boundary).
144. tests/test_hankin.h — verifies array sizes/unit-length but not that static vertices are the actual edge midpoints; flat-vs-twisted threshold (`1e-3`) is an unmotivated magic number.
145. tests/test_color.h — `hue_rotate` gray-preservation/identity tolerances are very loose (48/64 of 255), smoke-grade not precision.
146. tests/test_harness.h — `HS_EXPECT_NEAR` always widens to `double`, masking float-resolution precision-loss in mixed expressions.
147. tests (structural) — no host-vs-device numerical parity for the ARM `lerp16`/`uqadd16` asm beyond the software reference; documented limitation (no device build in CI), not a fixable gap.
148. daydream/tests/color_parity_wasm.test.js:124 — only the palette path has absolute goldens; `oklab`/`lissajous` parity is WASM-vs-JS only, so a coordinated shared drift passes.
149. daydream/tests/segment_controller.test.js:43 — `onmessageerror` is wired into the FakeWorker but never delivered/asserted; out-of-order frames within a generation are untested.
150. daydream/tests/recorder.test.js — the empty-chunk (`size:0`) filter and the `targetHeight` downscale export path are untested (only the native-pin path).
151. daydream/tests/gui.test.js — lil-gui is fully stubbed, so the `getParameterDefinitions()→gui.add(min,max)` auto-generation (float→slider, bool→checkbox) — a documented risky area — is unverified.
152. daydream/tests/sidebar_logic.test.js — the per-resolution hydration fallback (an effect not in the active list, incl. one from `?effect=`, is replaced with the list's first) is untested anywhere (prevents a black canvas).
153. daydream/tests (hygiene) — `globalThis.window`/`document`/`Worker` stubs are saved/restored in recorder but not in state/gui/controller tests, relying on `node --test` per-file process isolation (brittle if the runner ever shares context).

---

## 5. Recommended Sequencing

- **First**: items 1–11 (High). Most are small, localized fixes; #10/#11 are pure-logic
  tests that close the highest-value coverage gap cheaply, and #4/#5 prevent
  intermittently-wrong frames.
- **Then**: the two cross-cutting *themes* in Medium — (a) **slider-range vs preset
  audit** (#24, and sweep every `registerParam` against its presets), and (b) **the
  daydream segmented-pipeline `pendingFrame`/`frameComposited` conflation** (#34 fixes #5
  and #34 together by having `composite()` return a blit count).
- **Opportunistic**: the Low list is dominated by un-named magic constants, three small
  "one primitive, N spellings" consolidations (axis-seed #83, start-capture #84,
  float-formatter #132, per-`Params` lerp #99), and tool-page lifecycle/a11y debt — ideal
  for incremental cleanup, none blocking.

The codebase is production-quality; this list is a polish-and-harden backlog, not a
remediation plan for broken software.
