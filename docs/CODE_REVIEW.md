# Holosphere + Daydream — Code Quality Review

**Date:** 2026-06-07
**Reviewer:** Expert C++/TypeScript code audit (fresh independent pass)
**Method:** 16 parallel component agents, each reading its assigned source in full and citing `file:line`, judged against the project's own stated philosophy — always-on fail-fast (`HS_CHECK` traps surviving `NDEBUG`, no stdio, `__builtin_trap`), 16-bit linear color with OKLab/OKLCH interpolation, compile-time `<W,H>` specialization, and a single partitioned arena with explicit `Arena&` plumbing. This pass **deliberately did not assume the prior review's "FIXED" annotations held**; every claim was re-derived from the code as it stands, and the highest-impact items (the antipodal `slerp` trap, the `assert`-vs-`HS_CHECK` seams, the `hankin` narrowing parity gap, the `truncate(50°)` units bug, the segmented-POV `x_` sharing, the deploy-gate provenance file, and the dead-slider cluster) were spot-verified by the lead reviewer against the actual source and git state.
**Scope:** ~33k lines of hand-written C++ engine (`core/`), 27 modern effects, 3 hardware drivers + `platform.h`, the WASM/embind bridge, a 24-module native test suite, the CMake/CI build; plus the sibling vanilla-JS `daydream` web simulator. Generated tables (`reaction_graph.cpp`, `color_luts.h`) and third-party code (`FastNoiseLite.h`) were judged on integration/generation correctness, not line-by-line.
**Out of scope (per review rules):** `effects_legacy.h` and `targets/Holosphere/Holosphere.ino`.

---

## Overall grade: **B+**

This remains an exceptional solo engineering project, and an independent re-derivation confirms the bones rather than undermining them. Across a very large surface the architecture is not just principled but *consistently applied*: every render class is templated on `<W,H>` for zero-overhead specialization; all internal color is 16-bit linear light with Ottosson-correct, achromatic-guarded OKLCH interpolation; memory is a single partitioned arena with explicit `(Arena&, Arena&)` plumbing and no hidden state; and the rendering core is a variadic `Pipeline<W,H,Filters...>` that performs automatic 2D↔3D coordinate coercion at compile time with no runtime cost. The performance engineering is expert-level — split sin/cos trig LUTs (~145× RAM win, [geometry.h:208-256](../core/geometry.h#L208-L256)), an anisotropic per-face distance LUT gated by a sign-and-margin validity check, Lipschitz-aware sphere tracing of warped SDFs, a branchless segmented ISR, inline 16-bit→wire packing that bypasses the CRGB intermediate, and a zero-copy WASM readback whose detachment contract genuinely holds. Two marquee correctness claims survive re-verification: the WASM build **does** carry `-fno-finite-math-only` immediately after `-ffast-math` ([CMakeLists.txt:57](../CMakeLists.txt#L57)), and the precomputed reaction-diffusion K-NN table is structurally clean (7680 rows × uniform K=6, all indices in range, no self-references). The 1,848-line README is a genuinely outstanding engineering document.

What holds it at **B+ rather than A−** is that this skeptical pass found the project's *own* fail-fast and hygiene disciplines applied **less completely than the self-review implied**, and surfaced a coherent cluster of open items — several of which contradict prior "FIXED" annotations:

1. **The "always-on `HS_CHECK`" doctrine is still contradicted by `assert()` at safety-critical seams.** The canvas sink's finite/range precondition ([filter.h:106](../core/filter.h#L106)), `StaticPalette::get` ([color.h:1053](../core/color.h#L1053), with *no* cold-path trap anywhere), and `FunctionRef::operator()` ([concepts.h:75](../core/concepts.h#L75)) all guard with `assert()`, stripped on the device. The canvas sink is the single seam between a NaN/OOB producer and an unchecked `bufs_[cur_][y*width+x]` write, and it does not exist on hardware.

2. ✅ ~~**A new fail-fast parity gap: `hankin.h` performs the exact narrowing casts `conway.h` was hardened against.** `narrow_index()` ([conway.h](../core/conway.h)) now traps every Conway operator's `size()-1 → int16/uint16` cast, but `hankin.h` ([hankin.h:115,155-158](../core/hankin.h#L115)) still uses raw `static_cast<uint16_t>`/`<int16_t>` on its output vertex/face indices — silent corruption on a `MAX_VERTS` bump.~~ **FIXED (2026-06-07):** `narrow_index()` moved from `conway.h` to the shared `mesh.h` `MeshOps` namespace (single guarded narrowing for both operator families), and `compile_hankin`'s output vertex (`static_vertices.size()-1`, `dynamic_vertices.size()`), midpoint, and `static_offset + dyn_idx` face-index casts now route through it ([hankin.h:115,154-163](../core/hankin.h#L115)). A `MAX_VERTS` bump past `INT16_MAX` now traps at the bench instead of silently corrupting topology.

3. **A confirmed latent units bug in the solids registry.** `truncatedIcosidodecahedron_truncate05_ambo_dual` and its sibling call `.truncate(50.0f * D2R)` ([solids.h:442,504](../core/solids.h#L442)) — i.e. `t = 0.873` — but the function *name* (`truncate05`) and `truncate`'s `[0..0.5]` contract ([conway.h:418-424](../core/conway.h#L418)) make clear the intent was `truncate(0.5)` (which special-cases to `ambo`). At `t > 0.5` the two edge-cut points cross past each other, producing inverted/degenerate cut faces.

4. ✅ ~~**A latent crash class in the math core.** `slerp(Vector)` on antipodal endpoints at `t≈0.5` falls to a lerp fallback that yields the *zero vector*, then calls strict `.normalized()`, which traps ([3dmath.h:896-910](../core/3dmath.h#L896)).~~ **FIXED (2026-06-07, commit 593d8a4):** the near-antipodal `slerp` branch now degrades via `normalized_or(…, v1)` to a stable unit direction; the hot path is untouched. Same strict-normalize-of-a-degenerate-vector hazard exists in `make_rotation` for near-collinear non-unit inputs ([3dmath.h:853](../core/3dmath.h#L853)) and the `Spherical(Vector)` ctor ([3dmath.h:271](../core/3dmath.h#L271)) — **validated and deliberately retained as correct fail-fast** (a non-unit input to `make_rotation` and an origin input to `Spherical` are contract violations / caller bugs, not legitimate geometric edges; see P1 #1).

5. **The live-art interface still accretes inert controls**, and several specific dead sliders are confirmed: MobiusGrid `Rings`/`Lines` and ShapeShifter `Twist` are overwritten every frame by `Animation::Mutation`; SplineFlow `Speed` is value-copied into a `Driver` and never re-read; MindSplatter `Particles` is output-only telemetry; IslamicStars `Duration` is registered but read nowhere; ChaoticStrings `Cycle Dur` is read once and `next_preset()` is a dead `TODO` stub. Whole-param preset-lerp clobber afflicts Flyby, Liquid2D, MeshFeedback, and DreamBalls.

6. **The provenance gate the prior review reported as fixed is inert.** `holosphere_wasm.sha` is **untracked** in the daydream repo (`?? holosphere_wasm.sha`), so CI's `actions/checkout` never sees it and the deploy gate falls back to engine HEAD with a warning ([deploy.yml:47-60](../../daydream/.github/workflows/deploy.yml)) — exactly the decoupled behavior the mechanism was meant to remove. (The prior review was honest that the file "activates on the next WASM install"; that install has not happened, so the guarantee is not in force.)

None of these is fatal; all are fixable; most are localized and latent at current parameters. The core (color, pipeline, memory, mesh kernel, WASM bridge, single-Teensy hardware path) is genuinely shippable. But the gap between the stated philosophy and its enforcement is wider than a self-review suggests, and the test suite that markets the traps hardest still exercises only ~9 of ~57 trap sites and feeds exactly one NaN.

---

## Dimension grades (aggregated across all components)

| Dimension | Grade | Justification |
|---|---|---|
| **Architecture & Design** | **A−** | The variadic EBO `Pipeline` with transparent `if constexpr` domain coercion ([filter.h:163-191](../core/filter.h#L163)), the Conway-operator algebra + fluent `SolidBuilder` ping-pong DSL ([solids.h:198-275](../core/solids.h#L198)), the "animations mutate state / effects read state and draw" separation, the CRTP reaction-diffusion base, the dual-mode single-`CMakeLists`, and the `HS_WASM_RESOLUTIONS` / `HS_EFFECT_LIST` X-macro SSOTs are elegant and orthogonal. The convention discipline across 60+ files is the standout. Docked by two brute-force effects bypassing the rasterizer, the GS-vs-BZ asymmetry (one hoisted its lattice rebuild, the other didn't), and effects leaking carousel/preset lifecycle into the effect body. |
| **Correctness & Robustness** | **B−** | The genuine limiter. Headline algorithms are right and most degenerate paths are handled, but this pass found real latent bugs clustered at the seams: antipodal `slerp` → trap ([3dmath.h:896](../core/3dmath.h#L896)), `truncate(50°)` degenerate geometry ([solids.h:442](../core/solids.h#L442)), `hankin` unguarded narrowing ([hankin.h:115](../core/hankin.h#L115)), the SDF distance-LUT validity margin too loose for large faces ([sdf.h:1183](../core/sdf.h#L1183)), `Motion::set_duration(0)` div-by-zero ([animation.h:854](../core/animation.h#L854)), and the deploy provenance gap. Most are latent at current parameters — which is exactly why the tests can't catch them. |
| **Performance & Efficiency** | **A−** | Microsecond-budget awareness almost everywhere: split sin/cos LUTs, anisotropic distance LUT with exact fallback, fused linear→sRGB wire packing, branchless segmented ISR, zero-alloc steady-state loops, single-draw-call instanced web mesh, exact `/65535` lerp avoiding the `>>16` darkening bias. Dragged by Metaballs/Voronoi `O(W·H·N)` per-pixel loops (Voronoi with two per-pixel `acosf`), GS's per-frame 7680-node rebuild that BZ already hoisted, and a per-pixel virtual `get_pixel` in the hardware ISR. |
| **Readability & Maintainability** | **A−** | Outstanding Doxygen and *why*-not-*what* comments at every subtle seam (the relaxed-atomic rationale, the buffer-detach contract, `Persist` member ordering). Held back by recurring copy-paste (the orbit/visited-verts boilerplate across 5 Conway operators, `DistortedRing`≈`Ring`) and several stale/contradictory comments (`meta`'s thinking-out-loud block at [conway.h:1009](../core/conway.h#L1009), `hs::log`'s "no vsnprintf" while calling it at [platform.h:48-57](../core/platform.h#L48), the LUT cell-diagonal claim at [sdf.h:1006](../core/sdf.h#L1006) vs the width-based code). |
| **Memory Safety & Resource Mgmt** | **B+** | Disciplined arena ownership; `Arena::allocate` (no-wrap subtractive bound) and `ArenaVector`/`bind` (the `SIZE_MAX/sizeof(T)` multiply guard) are model fail-fast citizens; `Persist`/`ScratchScope` RAII and Three.js GPU disposal-on-resize are correct. Undercut by `ArenaSpan` generation tracking that catches arena reset but **not** a `bind()`-driven re-grow of its source ([memory.h:417](../core/memory.h#L417)), the global block being a plain `uint8_t[]` whose partition boundaries aren't aligned, `check_bound` coverage gaps on `clear()`/`data()`, and the still-ungeneralized rvalue borrow guards on ~4 animations. |
| **API / Interface Expressiveness** | **B** | `Pipeline<W,H,...>`, `SolidBuilder`, `REGISTER_EFFECT`, the X-macro SSOTs, and `AppState`/`URLSync` are a genuine pleasure and largely misuse-resistant. Pulled down hard by the recurring dead/Mutation-clobbered/read-once/preset-clobbered effect sliders (the project's most persistent hygiene gap), `Timeline::add_get` handing out relocation-invalidatable raw pointers, and the untyped `setParameter` (unclamped NaN/Inf) and inconsistent `setClip`-aborts-vs-`setResolution`-rejects JS boundary. |
| **Testing** | **B** | Excellent native harness asserting *real* algebraic/topological invariants (Hamilton algebra, slerp composition, Euler V−E+F=2, half-edge symmetry, a machine-verified reaction-graph integrity battery, OKLab round-trips) and a death harness that now asserts the *specific* trap status (`SIGILL` / `STATUS_ILLEGAL_INSTRUCTION`) with a `__spawn_check__` control. But the marketed trap surface is barely covered: **~9 of ~57 `HS_CHECK` sites** have a death case, there is exactly **one** NaN-input fault test (`normalized()`), the entire death suite SKIPs green when `$CI` is unset, and the 64-bit/8MB host build makes the device's 32-bit layouts and 335KB ceiling structurally invisible. |
| **Documentation** | **A−** | The README remains a genuinely outstanding engineering document. Held just below A by code/doc drift this pass found: the `meta` operator comment, the stale `hs::log` "no vsnprintf" rationale, and the SDF LUT "cell diagonal" comment that the code contradicts. |

### Per-component snapshot

| Component | Arch | Correct | Perf | Mem | API | Overall | Standout |
|---|---|---|---|---|---|---|---|
| Core Math & Geometry | A− | B− | A | A | B+ | **B+** | `TrigLUT<W,H>` split-trig reconstruction (~145× RAM win) |
| Color & Palettes | A− | B | A | A | B+ | **B+** | Ottosson-exact OKLCH + achromatic/shortest-arc guards |
| SDF + Scan Rasterizer | A− | B | A− | A | B+ | **B+** | `scan_region` seam split/coalesce; correct full-width-B Subtract |
| Plot (Curve) Rasterizer | A− | B | A− | A | B+ | **B+** | Simulate-then-replay `sin(φ)` adaptive stepping |
| Pipeline / Filters / Canvas | **A** | B+ | A | A− | B+ | **A−** | EBO variadic pipeline + `if constexpr` domain dispatch |
| Animation / Transformers | A− | B | A− | A | C+ | **B+** | Deleted-rvalue borrow guards on `Lerp`/`Motion`/`MeshMorph` |
| Memory / Arena | A− | B+ | A | A− | B+ | **B+** | No-wrap subtractive `allocate` bound + `bind` multiply guard |
| Mesh / Conway / Hankin / Spatial / Solids | A− | B | A− | A | B+ | **B+** | `narrow_index` + non-manifold `pair_half_edges` traps |
| Effects A (RD / Hopf / Islamic / SH) | A− | B | B+ | — | C+ | **B** | CRTP RD base; BZ cubemap-LUT seed → K-NN refine |
| Effects B (Mesh / Field / Particle) | B+ | B | A− | — | C+ | **B** | Thrusters' deliberate no-Sprite ring-slot safety |
| Effects C (Shader / Curve / misc) | B+ | B− | C+ | — | C | **B−** | Liquid2D trig-free triple-θ mirror glitch lens |
| Hardware Drivers + Platform | A− | B | A | A | B+ | **B+** | Honest relaxed-atomic + explicit-cache-flush handoff |
| WASM Bridge + Reaction Graph | **A** | A− | A | A | B+ | **A−** | Dual-enforced resolution/effect X-macro SSOT |
| Tests | — | — | — | — | — | **B** | Specific-status death harness w/ spawn-check control |
| Build / CI / Cross-cutting | A− | C+ | — | — | — | **B−** | `-fno-finite-math-only` correctly ordered after `-ffast-math` |
| Daydream JS | A− | A− | A | A− | B+ | **A−** | Two-layer compositor fence (generation + per-result bounds) |

---

## Cross-cutting themes

### 1. Fail-fast is the headline philosophy, still enforced inconsistently

The lookup/registration/config/memory layers are genuinely fail-fast and verified: `Arena::allocate`/`ArenaVector` trap on OOM/overflow with no-wrap arithmetic, `bind()` guards the `exact_capacity*sizeof(T)` multiply, `Solids::get_by_name` traps on miss, `pair_half_edges` traps on a non-manifold edge fan, `SpatialHash::query` now traps on bucket overflow at parity with `insert`, and `conway.h`'s `narrow_index()` traps every operator's output-index narrowing. But three categories still slip:

| Category | Locations | Problem |
|---|---|---|
| **`assert()` where `HS_CHECK` was promised** | [filter.h:106](../core/filter.h#L106) (canvas-sink finite/range), [color.h:1053](../core/color.h#L1053) (`StaticPalette::get`, *no* cold trap at all), [concepts.h:75](../core/concepts.h#L75) (`FunctionRef`), [color.h:1215](../core/color.h#L1215) (`BakedPalette::get` null `lut_`) | Stripped under `NDEBUG` on device. The canvas sink is a *per-plotted-point boundary* (not the per-pixel inner loop), so a single `HS_CHECK` on the finite precondition there is contract-appropriate and is the highest-value conversion — it is the only thing between a bad producer and an unchecked write. `StaticPalette` is the worst: `bind()` doesn't trap and `get()` only asserts, so the device has no guard at all. |
| ✅ ~~**`hankin.h` narrowing parity gap**~~ *(FIXED 2026-06-07)* | [hankin.h:115,154-163](../core/hankin.h#L115) | The output vertex/face-index casts now route through `narrow_index` (relocated to the shared `mesh.h`), at parity with `conway.h` — a `MAX_VERTS` bump past `INT16_MAX` traps instead of silently corrupting topology. |
| **`Gradient` ctor + numerical-core silent paths** | `Gradient` ctor writes `entries[static_cast<int>(pos*255)]` with no bound on `pos` ([color.h:550](../core/color.h#L550)); strict `normalized()` on degenerate vectors in `slerp`/`make_rotation`/`Spherical` ctor; three inconsistent pole sentinels across `stereo`/`gnomonic`/`inv_stereo` | A position `>1` in a `Gradient` initializer is an OOB write with no trap; the antipodal `slerp` is an outright latent crash. |

This remains the single highest-leverage cluster: mechanical to fix, and it converts silent-wrong-data into bench-time crashes — the entire point of the doctrine.

### 2. The borrow-safety discipline is modeled but still not generalized

`Lerp`, `Motion`, `MobiusFlow`, and `MeshMorph` correctly `=delete` their rvalue ctor overloads ([animation.h:727,842,1197,1372](../core/animation.h#L727)) — exemplary. Re-derivation confirms **most** other ref-holding animations are in fact temporary-safe by construction (`Transition`/`Mutation`/`Driver` take the mutated state by *non-const lvalue ref*, which already rejects bare temporaries; `ColorWipe`/`MeshMorph` snapshot/clone in the ctor; `Ripple`/`RandomWalk` copy their `Vector` arg). The residual hazard is the `std::ref(...)`-or-member-of-temporary path on `Transition`/`Mutation`/`Driver`/`MobiusWarp*`, which the deleted-overload trick would close at zero cost and which `MobiusFlow` already models. Separately — and more structural — `Timeline::add_get` returns a raw pointer into inline event storage that `step()` compaction relocates ([animation.h:1694](../core/animation.h#L1694)); Comets, HopfFibration, Liquid2D, SplineFlow, and ChaoticStrings all cache it and rely on an *unstated* ordering invariant ("all infinite anims added before any finite one"). It holds today in every case checked, but a single future finite anim inserted earlier dangles the handle.

### 3. Latent correctness at the hard seams

The remaining real bugs live where the sphere's topology and the math core bite:
- **Math core:** ✅ ~~antipodal `slerp(Vector)` → zero vector → `normalized()` trap~~ *(FIXED 2026-06-07: near-antipodal branch now uses `normalized_or(…, v1)`, [3dmath.h:904-910](../core/3dmath.h#L904))*; `Projection<uint8_t W,H>` silently truncates `W=288` to 32 with no `static_assert` ([rotate.h:38](../core/rotate.h#L38)); `Orientation::upsample(1)` divides by `count-1` ([geometry.h:498](../core/geometry.h#L498)).
- **Geometry registry:** `truncate(50.0f * D2R)` where the name says `0.5` → degenerate crossed cut faces ([solids.h:442,504](../core/solids.h#L442)).
- **SDF/CSG:** the anisotropic distance-LUT validity gate compares `min_abs` against a *canvas-width* margin (`1.8·2π/W`, [sdf.h:1183](../core/sdf.h#L1183)) rather than the true per-face LUT cell diagonal, so for medium/large faces the bilinear cell can straddle the zero-isocontour and mis-sign an edge; `Volume`'s back-of-sphere early-out is correct only under the (unguarded, documented) `bounds_center == center` + scale-free invariant ([scan.h:786](../core/scan.h#L786)).
- **Plot:** planar-mode sampling feeds an arc-length fraction into a projection-linear `map_planar`, so samples aren't arc-length-uniform and Star/Flower's integer-semantic `v2` is lerped to fractional values ([plot.h](../core/plot.h)).
- **Animation:** `Motion::set_duration(0)` bypasses the ctor's `duration==0?1` guard and feeds `t/0` into `path_fn` ([animation.h:854](../core/animation.h#L854); Comets wires a live slider to it).
- **Hardware:** `x_` is a plain `int` written in `show_col` and read in `frame_sync_isr` ([pov_segmented.h](../hardware/pov_segmented.h)) — safe today only by equal-NVIC-priority (IRQs don't preempt at equal priority), an undocumented and brittle invariant. The segment-0 self-pulse race is correctly resolved (`frame_sync_isr` early-returns on segment 0).
- **Web:** the segment compositor's stale-resolution OOB is correctly defended on two layers (generation fence + per-result bounds clamp) — verified fixed.

None bite at current parameters — which is exactly why they need tests, not just fixes.

### 4. The live-art interface keeps accreting inert controls

`registerParam` binds a live pointer; a slider is "live" iff `draw_frame` actually *reads* that member each frame. Confirmed dead/inert this pass:

| Effect | Slider(s) | Mechanism | Cite |
|---|---|---|---|
| MobiusGrid | `Rings`, `Lines` | `Animation::Mutation` overwrites every frame | [MobiusGrid.h:33-50](../effects/MobiusGrid.h#L33) |
| ShapeShifter | `Twist` | `Animation::Mutation` overwrites every frame | [ShapeShifter.h:42,77](../effects/ShapeShifter.h#L42) |
| SplineFlow | `Speed` | value-copied into `Driver`, never re-read / no `set_speed` | [SplineFlow.h:36,57](../effects/SplineFlow.h#L36) |
| MindSplatter | `Particles` | output-only telemetry, clobbered every frame | [MindSplatter.h:31,62](../effects/MindSplatter.h#L31) |
| IslamicStars | `Duration` (+ ripple defaults overwritten post-register) | registered, read nowhere; literals overwrite registered defaults | [IslamicStars.h:23,32-34](../effects/IslamicStars.h#L23) |
| ChaoticStrings | `Cycle Dur` (+ dead `next_preset()` stub) | read once into timer construction | [ChaoticStrings.h:74,119](../effects/ChaoticStrings.h#L74) |
| Flyby / Liquid2D / MeshFeedback / DreamBalls | whole param set | continuous/periodic preset-lerp clobbers any user edit | e.g. [Flyby.h:41-47](../effects/Flyby.h#L41), [MeshFeedback.h:51-56](../effects/MeshFeedback.h#L51) |

The durable fix is the pattern the strong effects already use (per-frame re-read with a change gate, or drive a dedicated phase member and keep the registered member as a live multiplier) *plus* a debug "registered-but-unread param" lint so this is caught at build time rather than by audit.

### 5. The test suite does not exercise the philosophy it markets

The invariant tests are genuinely strong (Euler characteristic from the half-edge mesh, two-way winding, a machine-verified K-NN table, OKLab round-trips), and the death harness now asserts the *specific* trap status with a spawn-check control — both real improvements. But the always-on trap surface — the feature the project foregrounds — is still barely covered: **~9 of ~57 `HS_CHECK` sites** have a death case (whole subsystems — conway, scan, hankin, canvas — have zero), there is exactly **one** non-finite-input fault test (`normalized()` on a NaN), nothing feeds a NaN to `slerp`/`make_rotation`/`make_basis`, the death suite SKIPs *green* when `$CI` is unset, and the effect smoke test asserts only accessor stability (an all-black buffer passes). The host build is 64-bit on an ~8MB arena, so the device's 32-bit layouts and 335KB ceiling are structurally unrepresentable; a per-effect footprint that bricks the device passes here.

---

## Standout engineering

Excellence should be named, not just defects:

- **The variadic filter pipeline** ([filter.h:163-191](../core/filter.h#L163)). `Pipeline<W,H,Head,Tail...>` derives from `Head` (EBO, zero storage) and dispatches `plot()` via `if constexpr (Head::is_2d)`, transparently lifting 2D↔3D coordinates at the mismatch boundaries with no runtime cost — the cleanest expression of the engine's compile-time-resolution thesis, funneled through one wrap+clip+blend sink.
- **The anisotropic distance LUT with a validity gate** ([sdf.h:1462-1492](../core/sdf.h#L1462)). Bilinear interpolation is trusted only when all four corners share sign *and* the smallest magnitude exceeds a safe distance; otherwise it falls back to exact edge evaluation. The idea is textbook-correct (the only flaw is the margin scaling, theme 3).
- **The honest relaxed-atomic rationale** ([dma_led.h:286-304](../hardware/dma_led.h#L286)). It separates the single-observer ISR/thread flag (no barrier on a single core) from DMA buffer coherence (handled by `arm_dcache_flush_delete` + DSB) and warns against a bogus acquire/release "fix". Staff-level reasoning that is *correct* — do not "fix" it.
- **OKLab/OKLCH interpolation with achromatic guards and an exact 16-bit lerp** ([color.h:418-494](../core/color.h#L418), [color.h:119](../core/color.h#L119)). Ottosson-exact coefficients, near-zero-chroma special-casing before the ±π hue wrap, and a `(x + (x>>16) + 1) >> 16` divide that avoids the `>>16` darkening bias.
- **The no-wrap arena bounds check** ([memory.h:72](../core/memory.h#L72)). `padding > capacity - offset || size > capacity - offset - padding` cannot wrap (because `offset <= capacity` is invariant) and catches a colossal `size` from an overflowed `bind()` multiply that an additive form would miss — paired with the `SIZE_MAX/sizeof(T)` multiply guard, the two layers genuinely close the overflow path.
- **The dual-enforced X-macro SSOTs** ([wasm.cpp:90-115](../targets/wasm/wasm.cpp#L90), [effects.h](../core/effects.h)). `setResolution`/`setEffect`/`getEffectSizes` all expand from `HS_WASM_RESOLUTIONS`, and the effect roster is enforced three ways — a startup `HS_CHECK(registry.size()==HS_EFFECT_COUNT)`, the native smoke suite, and the Phantasm firmware show-list — so the supported set provably cannot drift.
- **`SolidBuilder`** ([solids.h:198-275](../core/solids.h#L198)). Each Conway op runs `op(mesh,*a,*b)` then `std::swap(a,b)`, hiding a two-arena ping-pong behind a fluent chain so Islamic-pattern recipes read as a declarative geometry DSL while staying single-allocation and arena-deterministic.
- **The two-layer daydream compositor fence** ([segment_controller.js](../../daydream/segment_controller.js)). A monotonic `renderGen`/`inflightGen` generation fence drops stale-resolution worker frames *and* a per-result bounds clamp backstops it, so a mid-flight resolution change cannot OOB-write the resized buffer — the prior review's flagged race is genuinely defended.

---

## Prioritized fix list

### P0 — None found

No crash-on-normal-use or shipped-artifact-corrupts-output defect bites at current parameters. The antipodal-`slerp` trap (below) is the closest, but requires an exact-antipode pair at `t≈0.5`.

### P1 — Latent crashes, fail-fast gaps, and confirmed defects

1. ✅ **Guard the strict-normalize degenerate paths in the math core.** **FIXED (2026-06-07):** `slerp(Vector)` returned the zero vector for antipodal endpoints at `t≈0.5` (reachable via `slerp(v, NORTH, …)` with a south-pole `v` in [styles.h:149](../core/styles.h#L149), per-vertex mesh morph, orientation upsample) then trapped in `.normalized()`; the near-antipodal branch now degrades via `normalized_or(…, v1)` to a stable unit direction ([3dmath.h:904-910](../core/3dmath.h#L904)). The hot path (the general `s1·v1 + s2·v2` normalize) is untouched — the change is confined to the cold near-antipodal branch, so no render-path regression. New regression assertions in `test_vector_slerp` cover the antipodal endpoints and midpoint. **The other three sub-items were validated and deliberately left as-is:** `make_rotation(from,to)` ([3dmath.h:853](../core/3dmath.h#L853)) documents a *unit-vector* contract under which the `d`-based parallel/antiparallel guards handle every degeneracy — line 853 traps only on a non-unit (contract-violating) input, which is *correct* fail-fast; the `Spherical(Vector)` ctor ([3dmath.h:271](../core/3dmath.h#L271)) traps on the origin, which has no defined direction (a caller bug, not a legitimate edge); and the suggested `static_assert(W<=255)` on `Projection<uint8_t W,H>` ([rotate.h:38](../core/rotate.h#L38)) is ineffective (the `uint8_t` template parameter truncates `288→32` *before* any in-class assert) and `Projection` is used only in out-of-scope `effects_legacy.h`.

2. **Promote the cold-boundary `assert()` seams to `HS_CHECK` (or move the check to the cold producer).** The canvas-sink finite/range precondition ([filter.h:106](../core/filter.h#L106)) is a per-point boundary, not the per-pixel loop, and is the only guard between a NaN/OOB producer and an unchecked write — convert at least the `isfinite` check. Add a cold `HS_CHECK` in `StaticPalette::bind()` ([color.h:1045](../core/color.h#L1045)) (it has *no* device guard today) and a null-`lut_` trap in `BakedPalette`. Clamp `Gradient` stop positions to `[0,1]` before indexing `entries[]` ([color.h:550](../core/color.h#L550)).

3. ✅ ~~**Route `hankin.h`'s output-index narrowing through `narrow_index`** ([hankin.h:115,155-158](../core/hankin.h#L115)) for parity with `conway.h` — converts a silent-corruption-on-budget-bump class into a bench trap.~~ **FIXED (2026-06-07):** `narrow_index` relocated to the shared `mesh.h` and applied to `compile_hankin`'s output vertex/face-index casts ([hankin.h:115,154-163](../core/hankin.h#L115)).

4. **Fix the `truncate(50.0f * D2R)` units bug** ([solids.h:442,504](../core/solids.h#L442)): the function names (`truncate05`) and `truncate`'s `[0..0.5]` contract say the intent is `truncate(0.5f)` (→ `ambo`); `0.873` produces degenerate crossed cut faces. Either pass `0.5f` or clamp `t` to `[0,0.5]` in `truncate` and trap out-of-range.

5. **Eliminate the confirmed dead sliders** (theme 4 table): drive a dedicated phase member from `Mutation`/`Driver` and keep the registered member as a live multiplier (MobiusGrid, ShapeShifter, SplineFlow); re-read IslamicStars `Duration` and ChaoticStrings `Cycle Dur` each frame (and delete the dead `next_preset()` stub); make MindSplatter `Particles` read-only telemetry; pause preset-lerp while a slider is being dragged (Flyby/Liquid2D/MeshFeedback/DreamBalls). Add a debug "registered-but-unread param" lint.

6. **Commit the deploy provenance file and verify the artifact.** `holosphere_wasm.sha` is untracked in daydream, so the SHA-pinned gate ([deploy.yml:47-60](../../daydream/.github/workflows/deploy.yml)) silently falls back to engine HEAD; commit it (the WASM install writes it) and add a step asserting the committed `holosphere_wasm.wasm` was built from that SHA, or rebuild in the gate.

### P2 — Robustness, memory, performance, and quality

7. **Add `=delete` rvalue overloads to the residual ref-holding animations** — `Transition`, `Mutation`, `Driver`, `MobiusWarp`/`MobiusWarpCircular`/`MobiusWarpEvolving` ([animation.h](../core/animation.h)) — closing the `std::ref`/member-of-temporary path that `MobiusFlow` already guards. Compile-time only.

8. ✅ ~~**Replace `Timeline::add_get`'s raw-pointer handle with a stable slot id + generation** ([animation.h:1694](../core/animation.h#L1694)), or pin events so compaction can't relocate a cached handle.~~ **FIXED — invariant made fail-fast (trap-on-move), 2026-06-07.** Validation reframed the fix: the dangling hazard is **real but dormant** — every *retained*-handle caller (Comets, HopfFibration, Liquid2D, SplineFlow) is safe by the unstated "infinite animation added before any finite one → compaction never shifts it" invariant. Crucially, `TransformerPool::spawn` *also* routes through `add_get` for its often-**finite, fire-and-forget** animations (IslamicStars ripples, FlowField particles), whose returned pointer is transient and discarded — so "handed out a pointer" ≠ "retains the pointer." The stable-id/generation rewrite was **declined** for a dormant bug: it's multi-mechanism (the on-demand-handle callers want a lookup, but GnomonicStars registers `&anim->speed` *into the GUI param system* and needs a **stable address**, which an id+lookup can't provide), and it touches the most-used animation machinery. Instead the invariant is now **enforced**: `TimelineEvent` gains a `handled` flag (free — it occupies existing alignment padding before `storage`); `add_get(…, pin=true)` (default) sets it for retained handles; `TransformerPool::spawn` passes `pin=false` for its transient pointers; and the single compaction chokepoint `TimelineEvent::move_into` traps via `HS_CHECK(!handled)` if a future change ever relocates a retained handle — a bench-time crash instead of a silent use-after-free, matching the project's hardware fail-fast doctrine. New death case `timeline_handled_relocation` proves the trap fires (10th case); all effects render unchanged. **Second sub-item also FIXED (2026-06-07):** `Motion::set_duration` wrote `this->duration` directly, bypassing the base ctor's existing `duration == 0 ? 1` guard and risking a `t / duration` divide-by-zero (→ NaN progress) on a live `0` from a slider; it now applies the same `0 → 1` rule (preserving the `-1` infinite sentinel). The sibling `PeriodicTimer` clamps `period` to `>= 1` in both its ctor and `set_period` (a `0`/negative period fires the callback every frame and self-retriggers). Behavior-neutral for all non-degenerate values; Comets renders identically.

9. **Scale the SDF distance-LUT validity margin by the true cell diagonal**, not canvas width ([sdf.h:1183](../core/sdf.h#L1183) vs the gate at [sdf.h:1474](../core/sdf.h#L1474)); add an `HS_CHECK(bounds_center==center)` (or recompute in local units) for the `Volume` early-out ([scan.h:786](../core/scan.h#L786)).

10. **Close the remaining arena edges**: stamp `ArenaSpan` against a per-vector rebind counter so a `bind()`-driven re-grow is detected ([memory.h:417](../core/memory.h#L417)); declare the global block `alignas(std::max_align_t)` and align partition boundaries in `configure_arenas`; add `check_bound` to `clear()`/`data()` ([memory.h:377](../core/memory.h#L377)).

11. **Replace the two `O(W·H·N)` brute-force effects** ([Metaballs.h:70-84](../effects/Metaballs.h#L70), N≤50; [Voronoi.h:63-119](../effects/Voronoi.h#L63), N≤400 + per-pixel `acosf`) with bounded-AABB rasterization (Metaballs) and a `spatial.h` KD-tree nearest+second query (Voronoi). **Hoist GS's per-frame 7680-node `build_nodes`** into `init()` ([GSReactionDiffusion.h:136](../effects/GSReactionDiffusion.h#L136)), as BZ already does.

12. **Harden the JS boundary**: clamp `setParameter` to the advertised `min/max` and reject NaN/Inf ([wasm.cpp:259](../targets/wasm/wasm.cpp#L259)); make `setClip` reject-and-return like `setResolution` instead of hard-aborting; validate the URL `effect` against the allow-list before `setEffect` ([daydream.js:318](../../daydream/daydream.js#L318)). Stop the recorder advancing/capturing while paused and re-sync the offscreen canvas on resize ([recorder.js](../../daydream/recorder.js)).

13. **Document/guard the hardware `x_` sharing**: `static_assert` or comment the equal-NVIC-priority invariant that makes the plain-`int` `x_` race-free across `show_col`/`frame_sync_isr` ([pov_segmented.h](../hardware/pov_segmented.h)); add a compile-time size guard on the DMA composite buffer.

### P3 — Polish

14. **Strip stale/contradictory comments** (per project convention): the `meta` thinking-out-loud block ([conway.h:1009](../core/conway.h#L1009)), the `hs::log` "no vsnprintf" rationale that calls `vsnprintf` ([platform.h:48](../core/platform.h#L48)), and the SDF "cell diagonal" comment that the width-based code contradicts ([sdf.h:1006](../core/sdf.h#L1006)).

15. **Harden the test suite's trap coverage**: add death cases for the conway/scan/hankin/canvas traps reachable without full render state, feed NaN/Inf to `slerp`/`make_rotation`/`make_basis` and assert a fault, and make the `$CI`-unset path fail loud rather than green-skip.

16. **Devirtualize the per-pixel `get_pixel` in the ISR**, factor the duplicated Conway orbit-emit boilerplate, and add a convergence/early-out to `relax` instead of a fixed iteration count.

---

## Comparative appraisal

**Versus hobbyist / maker LED-art projects (the immediate peer group):** Not comparable — several leagues above. The typical FastLED POV project is a single `.ino` of gamma-corrected 8-bit blends, `delay()`-driven timing, and copy-pasted effect loops. Holosphere has a typed compile-time rendering architecture, perceptual color science, an arena memory model with fail-fast invariants, a half-edge mesh kernel with the full Conway-operator algebra, a WASM digital twin, and a real invariant-driven test suite. Top-0.1% for the category.

**Versus professional / commercial offerings:** It compares favorably with the *engine* quality of commissioned generative-LED installation software. Where a shipping product pulls ahead is the operational envelope this project hasn't needed: CI on real hardware, a test suite that actually exercises the safety traps it markets, the *consistent* enforcement of a stated invariant (rather than `assert()` at the canvas sink and a parity gap in `hankin`), a content-creation tool that isn't five hand-rolled HTML pages, and the QA pass that would have caught the dead sliders, the `truncate(50°)` units slip, and the uncommitted provenance file before they shipped. The *core* is genuinely shippable; the *seams* are where it reads as one expert's project rather than a team's product.

**Versus academic work:** The numerical maturity (matched projection sentinels, analytic Lipschitz bounds for safe sphere-tracing of warped SDFs, OKLab/OKLCH shortest-arc interpolation, Conway-operator + Hankin-pattern geometry, a provably-correct Fibonacci-lattice K-NN graph, a stable spherical-harmonic recurrence) is at or above a strong graphics/geometry-processing course project or a SIGGRAPH-adjacent demo. What it lacks is the academic apparatus — no novelty claim, no baseline evaluation, no ablation. It is *engineering* of known-good algorithms executed unusually well, not a research contribution. As the artifact accompanying a thesis on real-time spherical rendering it would be a standout.

**Versus the state of the art:** The individual techniques are well-chosen current practice rather than frontier (split LUTs, instanced rendering, embind zero-copy, EBO variadic templates, sphere tracing). The *synthesis* — one unchanged C++ engine driving both a microsecond-budget ISR firmware on a multi-Teensy rig and a browser digital twin from a single source — is genuinely sophisticated systems design and is where the project is most impressive relative to anything off-the-shelf.

### Technical and artistic merit

**Technically**, this is the work of an expert who understands the whole stack — Cortex-M7 cache coherency, template metaprogramming, color appearance models, computational geometry, the WASM/JS FFI — and who maintains a *consistent design philosophy* across most of it. The recurring weakness is not ignorance but a boundary/enforcement problem: the fail-fast rule that governs the memory and lookup layers is contradicted by `assert()` at the canvas sink and a narrowing gap in `hankin`; the borrow-safety pattern that hardens four animations is absent from the `std::ref` path of four more; and the test suite never proves the traps fire. Close those gaps — `HS_CHECK` at the marketed seams, `narrow_index` in `hankin`, rvalue guards across the animation family, and a death-test/NaN-fault battery — and the correctness grade moves from B− toward A−, and the project with it.

**Artistically**, the effect catalog is ambitious and coherent: reaction-diffusion on a Fibonacci-lattice graph, the Hopf fibration with a 4D tumble, procedurally-generated authentic Islamic star patterns via Hankin's method, volumetric raymarched metallic tori at the vertices of a dodecahedron, Möbius-warped wireframes, real spherical-harmonic lobes with zero-crossing crossfade. These are not stock patterns, and the engine primitives are expressive enough that the strongest effects (`Raymarch`, `MeshFeedback`, `HopfFibration`, the Liquid2D/Flyby shaders) achieve real visual sophistication in ~25–80 lines apiece. The medium itself — a 480-RPM persistence-of-vision sphere painting full-color 16-bit-linear imagery — is novel, and the engineering is in genuine service of the art.

**Bottom line:** **B+.** A remarkable, near-professional codebase. The architecture, performance engineering, and documentation are A−-grade and the core is shippable. But this independent pass — which did not take the prior review's "FIXED" annotations on faith — found the project's own marquee discipline applied less completely than advertised: `assert()` at the canvas sink the device strips, a narrowing-cast parity gap in `hankin`, a confirmed `truncate(50°)` units bug, an antipodal-`slerp` latent crash, a borrow-guard pattern modeled but not generalized, a persistent dead-slider hygiene gap, a provenance gate that is inert because its file was never committed, and a test suite that exercises ~9 of ~57 traps and feeds one NaN. None of it is architectural — it is one philosophy, *consistently enforced* and *actually tested*, away from an unqualified A−.
