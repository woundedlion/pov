# CMake / build outputs
/build/
/build_debug/
/build_release/
/build_test/
/build_test_cmake/
*.obj
*.o
*.pdb
*.ilk
build.log

# CMake user presets (machine-local overrides)
CMakeUserPresets.json

# Dependencies / generated
/node_modules/
/.doxygen-awesome/
# Machine-local Doxygen config the justfile 'docs' recipe feeds to doxygen
# (analogous to CMakeUserPresets.json / .doxygen-awesome above).
/Doxyfile.local

# IDE / tooling
.vs/
/out/
/obj/
# Visual Micro generated state + per-user VS project settings. Un-anchored so it
# matches under targets/ (the root-anchored /__vm/ did not), and *.vcxproj.user
# was previously tracked only because it predated these rules.
__vm/
*.vcxproj.user
.clangd

# Python bytecode cache
__pycache__/
*.pyc
