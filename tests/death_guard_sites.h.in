/*
 * Required Notice: Copyright 2025 Gabriel Levy. All rights reserved.
 * Licensed under the PolyForm Noncommercial License 1.0.0
 *
 * GENERATED into the build tree by tests/count_guard_sites.cmake from
 * tests/death_guard_sites.h.in — re-derived at every configure, never edited.
 *
 * Per-basename census of the engine's HS_CHECK fail-fast sites, consumed by
 * the death harness's coverage report (tests/test_death.h).
 */
#pragma once

namespace hs_test {
namespace death_tests {

/** @brief One source file's fail-fast guard count. */
struct GuardSiteCount {
  const char *file; /**< Source-file basename, as check_fail() logs it. */
  int sites;        /**< HS_CHECK sites in it. */
};

/** Every guard-bearing engine file, in path order. */
inline constexpr GuardSiteCount GUARD_SITE_COUNTS[] = {
@HS_GUARD_SITE_ROWS@};

/** Total HS_CHECK sites across all of them. */
inline constexpr int GUARD_SITE_TOTAL = @HS_GUARD_SITE_TOTAL@;

} // namespace death_tests
} // namespace hs_test
