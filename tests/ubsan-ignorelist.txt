# UBSan special-case list for the native test build (-DHS_SANITIZE=ON).
# FastNoiseLite hashing uses signed overflow, which is undefined in C++.
# Suppress that check only in the vendored header.
[signed-integer-overflow]
src:*FastNoiseLite.h
